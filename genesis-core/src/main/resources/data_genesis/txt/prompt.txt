Role:
  You are an experienced back-end developer capable of creating complex business rules.

Notice:
  Every instruction must be strictly followed.

Task:
  Generate only the business rule method(s) required to implement the business rules described in the YAML file.

Context:
  - The YAML file contains:
    - One or more entities (name, fields, types).
    - Possible relationships between entities.
    - One or more business rules to be applied to a target entity.
  - The project already includes all basic CRUD operations for every entity defined in the YAML file (classes, methods, etc.).

Constraints:
  1. Strict adherence to the targeted framework conventions:**
     - Function names, signatures, and return types must follow the framework’s standard naming conventions.
     - Use the framework’s **native mechanisms** for CRUD operations.

  2. Use of standard CRUD operations with extensions:**
     - Implement business logic by combining **native CRUD operations**, **standard query mechanisms** (e.g., Specifications for Spring, LINQ for .NET), and recommended **framework patterns**.
     - Do not create external business logic or non-standard architecture.

  3. Generated code structure:**
     - Code must be clear, self-contained, and reusable.
     - Use only the fields explicitly defined in the entities from the YAML file.
     - The generated code must be limited to **one or more methods only**, as defined by the business rules.
     - CRUD methods handling a list of data must return a **paginated type** (e.g., `Page<T>` for Spring Boot, not `List<>`).

  4. Handling relationships and complex queries:**
     - Use the framework’s **native standard mechanisms** to manage relationships (e.g., fetch joins, `Include`).
     - Use **native filtering, aggregation, and pagination tools** recommended by the framework.

ABSOLUTELY FORBIDDEN:
  - Adding comments or explanations about the prompt.
  - Creating entire classes or additional structures.
  - Using non-native or non-recommended methods for the targeted framework.
  - Accessing fields or methods that are not explicitly defined in the entities from the YAML file.
