- id: 1
  frameworkId: 1
  name: "Spring Security - JWT"
  securityFiles:
    - fileType: "Roles Entity"
      fileName: "RoleEntity"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.models;

        import jakarta.persistence.*;
        import lombok.Getter;
        import lombok.Setter;

        @Getter
        @Setter
        @Entity
        @Table(name = "roles")
        public class RoleEntity {
            @Id
            @GeneratedValue(strategy = GenerationType.IDENTITY)
            @Column(name = "id")
            private Long id;

            @Column(name = "name")
            private String name;
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/models"

    - fileType: "Role User Repository"
      fileName: "RoleEntityRepository"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.repositories;
  
        import ${groupLink}.${lowerCase(projectName)}.models.RoleEntity;
        import org.springframework.data.jpa.repository.JpaRepository;
        import java.util.Optional;
        
        public interface RoleEntityRepository extends JpaRepository<RoleEntity, Long> {
          Optional<RoleEntity> findAllByName(String username);
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/repositories"

    - fileType: "User Entity"
      fileName: "UserEntity"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.models;

        import jakarta.persistence.*;
        import lombok.Getter;
        import lombok.Setter;

        @Getter
        @Setter
        @Entity
        @Table(name = "users")
        public class UserEntity {
            @Id
            @GeneratedValue(strategy = GenerationType.IDENTITY)
            @Column(name = "id")
            private Long id;

            @Column(name = "name")
            private String name;

            @Column(name = "email")
            private String email;

            @Column(name = "password")
            private String password;

            @ManyToOne
            @JoinColumn(name = "role_id")
            private RoleEntity roleidRoleEntity;
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/models"

    - fileType: "User Repository"
      fileName: "UserEntityRepository"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.repositories;

        import ${groupLink}.${lowerCase(projectName)}.models.UserEntity;
        import org.springframework.data.jpa.repository.JpaRepository;
        import java.util.Optional;

        public interface UserEntityRepository extends JpaRepository<UserEntity, Long> {
            Optional<UserEntity> findAllByName(String name);
            UserEntity findUserByName(String name);
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/repositories"

    - fileType: "User table and data init script"
      fileName: "users"
      extension: "sql"
      content: |
        --Default login
        --Username: admin
        --Password: admin
        {{#if databaseType = PostgreSQL}}
        CREATE TABLE IF NOT EXISTS roles
        (
          id SERIAL PRIMARY KEY,
          name VARCHAR(100) NOT NULL UNIQUE
        );
        
        CREATE TABLE IF NOT EXISTS users
        (
          id SERIAL PRIMARY KEY,
          name VARCHAR(100) UNIQUE NOT NULL,
          email VARCHAR(100) UNIQUE NOT NULL CHECK (email ~ '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'),
          password VARCHAR(100) NOT NULL,
          role_id INTEGER NOT NULL REFERENCES roles(id)
        );
        
        INSERT INTO roles (name) VALUES ('ROLE_ADMIN')
        ON CONFLICT (name) DO NOTHING;
        
        INSERT INTO users (name, email, password, role_id)
        VALUES ('admin', 'admin@user.com', '$2a$10$GBvWdFZht2UZ8eOEXnLv.e/txUqjTUVnyotHTEtA.V9SAwVs6sgTS', 1)
        ON CONFLICT (email) DO NOTHING;
        {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = MySQL}}
        CREATE TABLE IF NOT EXISTS roles (
          id INT AUTO_INCREMENT PRIMARY KEY,
          name VARCHAR(100) NOT NULL UNIQUE
        );
        
        CREATE TABLE IF NOT EXISTS users (
          id INT AUTO_INCREMENT PRIMARY KEY,
          name VARCHAR(100) NOT NULL UNIQUE,
          email VARCHAR(100) NOT NULL UNIQUE,
          password VARCHAR(100) NOT NULL,
          role_id INT NOT NULL,
          CONSTRAINT fk_type_user FOREIGN KEY (role_id) REFERENCES roles(id),
          CONSTRAINT chk_email CHECK (email REGEXP '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$')
        );
        
        INSERT INTO roles (name) VALUES ('ROLE_ADMIN')
        ON DUPLICATE KEY UPDATE id=id;
        
        INSERT INTO users (name, email, password, role_id)
        VALUES ('admin', 'admin@user.com', '$2a$10$GBvWdFZht2UZ8eOEXnLv.e/txUqjTUVnyotHTEtA.V9SAwVs6sgTS', 1)
        ON DUPLICATE KEY UPDATE id=id;
        {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = SQL Server}}
        CREATE TABLE IF NOT EXISTS roles (
          id INT IDENTITY(1,1) PRIMARY KEY,
          name VARCHAR(100) NOT NULL UNIQUE
        );
        
        CREATE TABLE IF NOT EXISTS [users] (
          id INT IDENTITY(1,1) PRIMARY KEY,
          name VARCHAR(100) NOT NULL UNIQUE,
          email VARCHAR(100) NOT NULL UNIQUE,
          password VARCHAR(100) NOT NULL,
          role_id INT NOT NULL,
          CONSTRAINT FK_user_type_user FOREIGN KEY (role_id) REFERENCES roles(id),
          CONSTRAINT CHK_email CHECK (email LIKE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}')
        );
        
        IF NOT EXISTS (SELECT 1 FROM roles WHERE name = 'ROLE_ADMIN')
        INSERT INTO roles (name) VALUES ('ROLE_ADMIN');
        
        IF NOT EXISTS (SELECT 1 FROM [users] WHERE name = 'admin' OR email = 'admin@user.com')
        INSERT INTO [users] (name, email, password, role_id)
        VALUES ('admin', 'admin@user.com', '$2a$10$GBvWdFZht2UZ8eOEXnLv.e/txUqjTUVnyotHTEtA.V9SAwVs6sgTS', 1);
        {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = Oracle}}
        --Default login
        --Username: admin
        --Password: admin
        CREATE TABLE roles (
        id  NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name VARCHAR2(100) NOT NULL UNIQUE
        ) TABLESPACE users;
        
        
        CREATE TABLE users (
        id  NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
        name VARCHAR2(100) NOT NULL UNIQUE,
        email VARCHAR2(100) NOT NULL UNIQUE,
        password VARCHAR2(100) NOT NULL,
        role_id NUMBER NOT NULL,
        CONSTRAINT fk_user_type_user FOREIGN KEY (role_id) REFERENCES roles(id),
        CONSTRAINT chk_email CHECK (REGEXP_LIKE(email, '^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$'))
        ) TABLESPACE users;
        
        
        MERGE INTO roles tu
        USING (SELECT 1 FROM dual) d
        ON (tu.name = 'ROLE_ADMIN')
        WHEN NOT MATCHED THEN
        INSERT (name) VALUES ('ROLE_ADMIN');
        
        MERGE INTO users u
        USING (SELECT 1 FROM dual) d
        ON (u.name = 'admin' OR u.email = 'admin@user.com')
        WHEN NOT MATCHED THEN
        INSERT (name, email, password, role_id)
        VALUES ('admin', 'admin@user.com', '$2a$10$GBvWdFZht2UZ8eOEXnLv.e/txUqjTUVnyotHTEtA.V9SAwVs6sgTS', 1);
        {{else}}{{removeLine}}{{/if}}
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/resources/sql-init/"

    - fileType: "Swagger Config"
      fileName: "SwaggerConfig"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config;

        import org.springframework.context.annotation.Configuration;
        import io.swagger.v3.oas.annotations.enums.SecuritySchemeType;
        import io.swagger.v3.oas.annotations.security.SecurityScheme;

        @Configuration
        @SecurityScheme(
            name = "bearerAuth",
            type = SecuritySchemeType.HTTP,
            scheme = "bearer",
            bearerFormat = "JWT"
        )
        public class SwaggerConfig {
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config"

    - fileType: "User role enumeration class"
      fileName: "UserRole"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.enums;
  
        import lombok.Getter;
        
        @Getter
        public enum UserRole {
          ADMIN("ROLE_ADMIN");
          
          private final String authority;
          
          UserRole(String authority) {
            this.authority = authority;
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/enums/"

    - fileType: "Custom implementation of the UserDetails(Spring security class)"
      fileName: "UserInfoDetails"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.models;
  
        import ${groupLink}.${lowerCase(projectName)}.models.UserEntity;
        import org.springframework.security.core.GrantedAuthority;
        import org.springframework.security.core.authority.SimpleGrantedAuthority;
        import org.springframework.security.core.userdetails.UserDetails;
        
        import java.util.Collection;
        import java.util.List;
        import java.util.stream.Collectors;
        import java.util.stream.Stream;
        
        public class UserInfoDetails implements UserDetails {
        
          private final String username; // Changed from 'name' to 'username' for clarity
          private final String password;
          private final String email;
          private final List<GrantedAuthority> authorities;
          
          public UserInfoDetails(UserEntity user) {
            this.username = user.getName(); // Assuming 'name' is used as 'username'
            this.password = user.getPassword();
            this.email = user.getEmail();
            this.authorities = Stream.of(user.getRoleidRoleEntity().getName().split(","))
            .map(SimpleGrantedAuthority::new)
            .collect(Collectors.toList());
          }
          
          @Override
          public Collection<? extends GrantedAuthority> getAuthorities() {
            return authorities;
          }
          
          @Override
          public String getPassword() {
            return password;
          }
          
          @Override
          public String getUsername() {
            return username;
          }
          
          @Override
          public boolean isAccountNonExpired() {
            return true; // Implement your logic if you need this
          }
          
          @Override
          public boolean isAccountNonLocked() {
            return true; // Implement your logic if you need this
          }
          
          @Override
          public boolean isCredentialsNonExpired() {
            return true; // Implement your logic if you need this
          }
          
          @Override
          public boolean isEnabled() {
            return true; // Implement your logic if you need this
          }
          
          public String getEmail() {
            return email;
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/models/"

    - fileType: "AuthRequest DTO"
      fileName: "AuthRequest"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.models;

        import jakarta.validation.constraints.NotBlank;

        public record AuthRequest(
                @NotBlank(message = "Username is required") String username,
                @NotBlank(message = "Password is required") String password
        ) {
            public String username() {
                return username;
            }

            public String password() {
                return password;
            }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/models/"

    - fileType: "PasswordChangeRequest DTO"
      fileName: "PasswordChangeRequest"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.models;

        import lombok.Getter;
        import lombok.Setter;

        @Getter
        @Setter
        public class PasswordChangeRequest {
            private String oldPassword;
            private String newPassword;
            private String confirmPassword;
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/models/"

    - fileType: "UserUpdateRequest DTO"
      fileName: "UserUpdateRequest"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.models;

        import lombok.Getter;
        import lombok.Setter;

        @Getter
        @Setter
        public class UserUpdateRequest {
            private String name;
            private String email;
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/models/"

    - fileType: "RefreshRequest DTO"
      fileName: "RefreshRequest"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.models;
        
        import jakarta.validation.constraints.NotBlank;
        
        public record RefreshRequest(@NotBlank String refreshToken) {}
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/models/"

    - fileType: "JwtService"
      fileName: "JwtService"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.services;
  
        import io.jsonwebtoken.Claims;
        import io.jsonwebtoken.Jwts;
        import io.jsonwebtoken.SignatureAlgorithm;
        import io.jsonwebtoken.io.Decoders;
        import io.jsonwebtoken.security.Keys;
        import jakarta.annotation.PostConstruct;
        import org.springframework.beans.factory.annotation.Value;
        import org.springframework.security.core.userdetails.UserDetails;
        import org.springframework.stereotype.Component;
        
        import java.security.Key;
        import java.util.Date;
        import java.util.HashMap;
        import java.util.Map;
        import java.util.function.Function;
        
        @Component
        public class JwtService {
          public static String SECRET;
          public static long EXPIRATION_TIME;
          @Value("$[jwt.secret]")
          private String secret;
          @Value("$[jwt.expiration]")
          private long expirationTime;
          
          // Get the signing key for JWT token
          private static Key getSignKey() {
            byte[] keyBytes = Decoders.BASE64.decode(SECRET);
            return Keys.hmacShaKeyFor(keyBytes);
          }
        
          // Extract the expiration date from the token
          public static Date extractExpiration(String token) {
            return extractClaim(token, Claims::getExpiration);
          }
        
          // Extract a claim from the token
          public static <T> T extractClaim(String token, Function<Claims, T> claimsResolver) {
            final Claims claims = extractAllClaims(token);
            return claimsResolver.apply(claims);
          }
        
          // Extract all claims from the token
          private static Claims extractAllClaims(String token) {
            return Jwts.parserBuilder()
            .setSigningKey(getSignKey())
            .build()
            .parseClaimsJws(token)
            .getBody();
          }
        
          @PostConstruct
          public void init() {
            SECRET = secret;
            EXPIRATION_TIME = expirationTime;
          }
        
          // Generate token with given user name
          public String generateToken(String userName) {
            Map<String, Object> claims = new HashMap<>();
            return createToken(claims, userName);
          }
        
          // Create a JWT token with specified claims and subject (user name)
          private String createToken(Map<String, Object> claims, String userName) {
            return Jwts.builder()
            .setClaims(claims)
            .setSubject(userName)
            .setIssuedAt(new Date())
            .setExpiration(new Date(System.currentTimeMillis() + 1000 * EXPIRATION_TIME)) // Token valid for 30 minutes
            .signWith(getSignKey(), SignatureAlgorithm.HS256)
            .compact();
          }
        
          // Extract the username from the token
          public String extractUsername(String token) {
            return extractClaim(token, Claims::getSubject);
          }
        
          // Check if the token is expired
          public Boolean isTokenExpired(String token) {
            return extractExpiration(token).before(new Date());
          }
        
          // Validate the token against user details and expiration
          public Boolean validateToken(String token, UserDetails userDetails) {
            final String username = extractUsername(token);
            return (username.equals(userDetails.getUsername()) && !isTokenExpired(token));
          }
        }

      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/services/"

    - fileType: "TokenBlacklistService"
      fileName: "TokenBlacklistService"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.services;
  
        import org.springframework.scheduling.annotation.Scheduled;
        import org.springframework.stereotype.Service;
        
        import java.util.concurrent.ConcurrentHashMap;
        
        @Service
        public class TokenBlacklistService {
          private final ConcurrentHashMap<String, Long> invalidatedTokens = new ConcurrentHashMap<>();
          
          public void invalidateToken(String token) {
            // Ajoute le token dans la blacklist avec une durée d'expiration correspondant à sa validité
            long expirationTime = extractExpiration(token);
            invalidatedTokens.put(token, expirationTime);
          }
          
          public boolean isTokenInvalidated(String token) {
            // Vérifie si le token est dans la blacklist
            Long expirationTime = invalidatedTokens.get(token);
            return expirationTime != null && expirationTime > System.currentTimeMillis();
          }
          
          private long extractExpiration(String token) {
            // Méthode pour extraire la date d'expiration du JWT (à implémenter avec JwtService)
            return JwtService.extractExpiration(token).getTime();
          }
          
          // Pour nettoyage automatique (facultatif)
          @Scheduled(fixedRate = 60000) // Chaque minute
          public void cleanExpiredTokens() {
            long now = System.currentTimeMillis();
            invalidatedTokens.entrySet().removeIf(entry -> entry.getValue() < now);
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/services/"

    - fileType: "UserInfoService"
      fileName: "UserInfoService"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.services;
  
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.AuthRequest;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.UserInfoDetails;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.UserUpdateRequest;
        import ${groupLink}.${lowerCase(projectName)}.models.UserEntity;
        import ${groupLink}.${lowerCase(projectName)}.repositories.UserEntityRepository;
        import org.springframework.beans.factory.annotation.Autowired;
        import org.springframework.context.annotation.Lazy;
        import org.springframework.security.authentication.AuthenticationManager;
        import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
        import org.springframework.security.core.Authentication;
        import org.springframework.security.core.userdetails.UserDetails;
        import org.springframework.security.core.userdetails.UserDetailsService;
        import org.springframework.security.core.userdetails.UsernameNotFoundException;
        import org.springframework.stereotype.Service;
        
        import java.util.Optional;
        
        import static ${groupLink}.${lowerCase(projectName)}.config.security.filters.SecurityConfig.passwordEncoder;
        
        @Service
        public class UserInfoService implements UserDetailsService {
          private final UserEntityRepository repository;
          private final JwtService jwtService;
          private final AuthenticationManager authenticationManager;
          
          @Autowired
          public UserInfoService(UserEntityRepository repository, JwtService jwtService, @Lazy AuthenticationManager authenticationManager) {
            this.repository = repository;
            this.jwtService = jwtService;
            this.authenticationManager = authenticationManager;
          }
          
          @Override
          public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException {
            Optional<UserEntity> userDetail = repository.findAllByName(username); // Assuming 'email' is used as username
          
            // Converting UserInfo to UserDetails
            return userDetail.map(UserInfoDetails::new)
              .orElseThrow(() -> new UsernameNotFoundException("User not found: " + username));
          }
        
          public String addUser(UserEntity user) {
              // Encode password before saving the user
              user.setPassword(passwordEncoder.encode(user.getPassword()));
              repository.save(user);
              return "User Added Successfully";
          }
          
          public String authenticateAndGetToken(AuthRequest authRequest) {
            System.out.println("Received username: " + authRequest.username());
            System.out.println("Received password: " + authRequest.password());
    
            Authentication authentication = authenticationManager.authenticate(
                    new UsernamePasswordAuthenticationToken(authRequest.username(), authRequest.password())
            );
            if (authentication.isAuthenticated()) {
                return jwtService.generateToken(authRequest.username());
            } else {
                throw new UsernameNotFoundException("Invalid user request!");
            }
          }
        
          public String refreshAccessToken(String oldToken) {
              // 1. extrait l’identité
              String username = jwtService.extractUsername(oldToken);
              if (username == null) throw new IllegalArgumentException("Invalid token");
          
              // 2. charge l’utilisateur
              UserDetails user = loadUserByUsername(username);
          
              // 3. valide encore la signature / expiration
              if (!jwtService.validateToken(oldToken, user))
              throw new IllegalArgumentException("Token validation failed");
          
              // 5. génère un nouveau
              return jwtService.generateToken(username);
          }
          
          // Ajoutez ces méthodes dans UserInfoService
          public void updateUser(String username, UserUpdateRequest updateRequest) {
            UserEntity user = repository.findUserByName(username);
            if (user == null) {
              throw new UsernameNotFoundException("User not found: " + username);
            }
            
            if (updateRequest.getName() != null) {
                user.setName(updateRequest.getName());
            }
            if (updateRequest.getEmail() != null) {
                user.setEmail(updateRequest.getEmail());
            }
                
            repository.save(user);
          }
        
          public void changeUserPassword(
                  String username, 
                  String oldPassword, 
                  String newPassword, 
                  String confirmPassword) {
            
            if (!newPassword.equals(confirmPassword)) {
                throw new IllegalArgumentException("New password and confirmation do not match");
            }
          
            UserEntity user = repository.findUserByName(username);
            if (user == null) {
              throw new UsernameNotFoundException("User not found: " + username);
            }
                
            // Vérifier l'ancien mot de passe
            if (!passwordEncoder.matches(oldPassword, user.getPassword())) {
              throw new IllegalArgumentException("Old password is incorrect");
            }
          
            user.setPassword(passwordEncoder.encode(newPassword));
            repository.save(user);
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/services/"

    - fileType: "JwtAccessDeniedHandler"
      fileName: "JwtAccessDeniedHandler"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.handlers;

        import java.io.IOException;

        import ${groupLink}.${lowerCase(projectName)}.dto.RestResponse;
        import org.springframework.http.HttpStatus;
        import org.springframework.http.MediaType;
        import org.springframework.security.access.AccessDeniedException;
        import org.springframework.security.web.access.AccessDeniedHandler;
        import org.springframework.stereotype.Component;

        import com.fasterxml.jackson.databind.ObjectMapper;

        import jakarta.servlet.http.HttpServletRequest;
        import jakarta.servlet.http.HttpServletResponse;
        
        @Component
        public class JwtAccessDeniedHandler implements AccessDeniedHandler {

            private final ObjectMapper objectMapper;

            // Injection de l'ObjectMapper via le constructeur
            public JwtAccessDeniedHandler(ObjectMapper objectMapper) {
                this.objectMapper = objectMapper;
            }

            @Override
            public void handle(HttpServletRequest request,
                               HttpServletResponse response,
                               AccessDeniedException ex) throws IOException {

                // Création de la réponse standardisée
                RestResponse<Void> restResponse = RestResponse.buildErrorResponse(
                    HttpStatus.FORBIDDEN,
                    ex.getMessage(),
                    null
                );

                // Conversion en JSON
                String jsonResponse = objectMapper.writeValueAsString(restResponse);

                // Configuration de la réponse HTTP
                response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                response.setStatus(HttpServletResponse.SC_FORBIDDEN);
                response.getWriter().write(jsonResponse);
            }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/handlers/"

    - fileType: "JwtAuthEntryPoint"
      fileName: "JwtAuthEntryPoint"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.handlers;

        import java.io.IOException;

        import ${groupLink}.${lowerCase(projectName)}.dto.RestResponse;
        import org.springframework.http.HttpStatus;
        import org.springframework.http.MediaType;
        import org.springframework.security.core.AuthenticationException;
        import org.springframework.security.web.AuthenticationEntryPoint;
        import org.springframework.stereotype.Component;

        import com.fasterxml.jackson.databind.ObjectMapper;

        import jakarta.servlet.http.HttpServletRequest;
        import jakarta.servlet.http.HttpServletResponse;
        
        @Component
        public class JwtAuthEntryPoint implements AuthenticationEntryPoint {

            private final ObjectMapper objectMapper;

            public JwtAuthEntryPoint(ObjectMapper objectMapper) {
                this.objectMapper = objectMapper;
            }

            @Override
            public void commence(HttpServletRequest request, 
                                 HttpServletResponse response,
                                 AuthenticationException ex) throws IOException {

                // Construire la réponse standardisée
                RestResponse<Void> restResponse = RestResponse.buildErrorResponse(
                    HttpStatus.UNAUTHORIZED,
                    ex.getMessage(),
                    null  // Aucune donnée supplémentaire
                );

                // Convertir en JSON
                String jsonResponse = objectMapper.writeValueAsString(restResponse);

                // Configurer la réponse HTTP
                response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
                response.getWriter().write(jsonResponse);
            }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/handlers/"

    - fileType: "JwtAuthenticationFilter"
      fileName: "JwtAuthenticationFilter"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.filters;
  
        import jakarta.servlet.FilterChain;
        import jakarta.servlet.ServletException;
        import jakarta.servlet.http.HttpServletRequest;
        import jakarta.servlet.http.HttpServletResponse;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.JwtService;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.TokenBlacklistService;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.UserInfoService;
        import ${groupLink}.${lowerCase(projectName)}.dto.RestResponse;
        import org.springframework.beans.factory.annotation.Autowired;
        import org.springframework.context.annotation.Lazy;
        import org.springframework.http.HttpStatus;
        import org.springframework.http.MediaType;
        import org.springframework.lang.NonNull;
        import org.springframework.security.authentication.UsernamePasswordAuthenticationToken;
        import org.springframework.security.core.context.SecurityContextHolder;
        import org.springframework.security.core.userdetails.UserDetails;
        import org.springframework.security.web.authentication.WebAuthenticationDetailsSource;
        import org.springframework.stereotype.Component;
        import org.springframework.web.filter.OncePerRequestFilter;
        
        import com.fasterxml.jackson.databind.ObjectMapper;
        
        import java.io.IOException;
        
        @Component
        public class JwtAuthenticationFilter extends OncePerRequestFilter {
          private final JwtService jwtService;
          private final TokenBlacklistService tokenBlacklistService;
          private final UserInfoService userInfoService;
          
          private final ObjectMapper objectMapper;
          
          @Autowired
          public JwtAuthenticationFilter(JwtService jwtService, TokenBlacklistService tokenBlacklistService, @Lazy UserInfoService userInfoService,
          ObjectMapper objectMapper) {
            this.jwtService = jwtService;
            this.tokenBlacklistService = tokenBlacklistService;
            this.userInfoService = userInfoService;
            this.objectMapper = objectMapper;
          }
        
          @Override
          protected void doFilterInternal(HttpServletRequest request, @NonNull HttpServletResponse response, @NonNull FilterChain filterChain)
          throws ServletException, IOException {
    
            String authHeader = request.getHeader("Authorization");
            String username = null;
            String jwt = null;
    
            if (authHeader != null && authHeader.startsWith("Bearer ")) {
              jwt = authHeader.substring(7);
    
              // Vérification blacklist
              if (tokenBlacklistService.isTokenInvalidated(jwt)) {
                sendErrorResponse(response,
                HttpStatus.UNAUTHORIZED,
                "Token has been invalidated");
                return;
              }
    
              try {
                username = jwtService.extractUsername(jwt);
              } catch (Exception e) {
                sendErrorResponse(response,
                  HttpStatus.UNAUTHORIZED,
                  "Invalid token: " + e.getMessage());
                return;
              }
            }

            if (username != null && SecurityContextHolder.getContext().getAuthentication() == null) {
                UserDetails userDetails = userInfoService.loadUserByUsername(username);
                
                try {
                    if (jwtService.validateToken(jwt, userDetails)) {
                        UsernamePasswordAuthenticationToken authToken = new UsernamePasswordAuthenticationToken(
                                userDetails, null, userDetails.getAuthorities());
                        authToken.setDetails(new WebAuthenticationDetailsSource().buildDetails(request));
                        SecurityContextHolder.getContext().setAuthentication(authToken);
                    }
                } catch (Exception e) {
                    sendErrorResponse(response, 
                                     HttpStatus.UNAUTHORIZED, 
                                     "Token validation failed: " + e.getMessage());
                    return;
                }
            }
            filterChain.doFilter(request, response);
          }

          private void sendErrorResponse(HttpServletResponse response, 
                                        HttpStatus status, 
                                        String message) throws IOException {
              RestResponse<String> errorResponse = RestResponse.buildErrorResponse(
                  status,
                  message,
                  null
              );
              
              response.setStatus(status.value());
              response.setContentType(MediaType.APPLICATION_JSON_VALUE);
              response.getWriter().write(objectMapper.writeValueAsString(errorResponse));
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/filters/"

    - fileType: "SecurityConfig"
      fileName: "SecurityConfig"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.filters;
  
        import ${groupLink}.${lowerCase(projectName)}.config.security.handlers.JwtAccessDeniedHandler;
        import ${groupLink}.${lowerCase(projectName)}.config.security.handlers.JwtAuthEntryPoint;
        import org.springframework.beans.factory.annotation.Autowired;
        import org.springframework.context.annotation.Bean;
        import org.springframework.context.annotation.Configuration;
        import org.springframework.security.authentication.AuthenticationManager;
        import org.springframework.security.authentication.AuthenticationProvider;
        import org.springframework.security.authentication.dao.DaoAuthenticationProvider;
        import org.springframework.security.config.annotation.authentication.configuration.AuthenticationConfiguration;
        import org.springframework.security.config.annotation.method.configuration.EnableMethodSecurity;
        import org.springframework.security.config.annotation.web.builders.HttpSecurity;
        import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
        import org.springframework.security.config.annotation.web.configurers.AbstractHttpConfigurer;
        import org.springframework.security.config.http.SessionCreationPolicy;
        import org.springframework.security.core.userdetails.UserDetailsService;
        import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
        import org.springframework.security.crypto.password.PasswordEncoder;
        import org.springframework.security.web.SecurityFilterChain;
        import org.springframework.security.web.access.expression.WebExpressionAuthorizationManager;
        import org.springframework.security.web.authentication.UsernamePasswordAuthenticationFilter;
        import org.springframework.web.cors.CorsConfiguration;
        import org.springframework.web.cors.CorsConfigurationSource;
        import org.springframework.web.cors.UrlBasedCorsConfigurationSource;
        
        import java.util.Arrays;
        import java.util.List;
  
        @Configuration
        @EnableWebSecurity
        @EnableMethodSecurity
        public class SecurityConfig {
          public static final PasswordEncoder passwordEncoder = new BCryptPasswordEncoder(); // Password encoding
          public final UserDetailsService userDetailsService;
          private final JwtAuthenticationFilter authFilter;
          
          //NEW
          private final JwtAuthEntryPoint authenticationEntryPoint;
          private final JwtAccessDeniedHandler accessDeniedHandler;
          
          @Autowired
          public SecurityConfig(JwtAuthenticationFilter authFilter, UserDetailsService userDetailsService,
            JwtAuthEntryPoint authenticationEntryPoint,
            JwtAccessDeniedHandler accessDeniedHandler) {
            this.authFilter = authFilter;
            this.userDetailsService = userDetailsService;
            
            this.authenticationEntryPoint = authenticationEntryPoint;
            this.accessDeniedHandler = accessDeniedHandler;
          }
        
          static CorsConfigurationSource getCorsConfigurationSource(CorsConfiguration configuration) {
                configuration.setAllowedMethods(Arrays.asList("GET", "POST", "PUT", "PATCH", "DELETE", "OPTIONS"));
                configuration.setAllowedHeaders(List.of("*"));
                configuration.setAllowCredentials(false);
                configuration.setMaxAge(3600L);
        
                UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
                source.registerCorsConfiguration("/**", configuration);
                return source;
          }
        
          @Bean
          public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
            http
              .cors(cors -> cors.configurationSource(corsConfigurationSource()))
              .csrf(AbstractHttpConfigurer::disable)
              .authorizeHttpRequests(auth -> auth
                  .requestMatchers("/auth/**", "/welcome/**", "/swagger-ui/**", "/v3/api-docs/**").permitAll()
                  // Autoriser ADMIN sur TOUTES les routes via une expression SpEL
                  .anyRequest().access(new WebExpressionAuthorizationManager(
                      "hasAuthority('ROLE_ADMIN')" 
                  ))
              )
              .sessionManagement(sess -> sess
                      .sessionCreationPolicy(SessionCreationPolicy.STATELESS) // No sessions
              )
              .authenticationProvider(authenticationProvider()) // Custom authentication provider
              .addFilterBefore(authFilter, UsernamePasswordAuthenticationFilter.class) // Add JWT filter
              .exceptionHandling()
                    .authenticationEntryPoint(authenticationEntryPoint)
                    .accessDeniedHandler(accessDeniedHandler);
            return http.build();
          }
      
          @Bean
          public CorsConfigurationSource corsConfigurationSource() {
            CorsConfiguration configuration = new CorsConfiguration();
            configuration.setAllowedOrigins(List.of("*")); // Spécifiez explicitement l'origine
            return getCorsConfigurationSource(configuration);
          }
    
          @Bean
            public PasswordEncoder passwordEncoder() {
            return passwordEncoder; // Password encoding
          }
    
          @Bean
          public AuthenticationProvider authenticationProvider() {
            DaoAuthenticationProvider authenticationProvider = new DaoAuthenticationProvider();
            authenticationProvider.setUserDetailsService(userDetailsService);
            authenticationProvider.setPasswordEncoder(passwordEncoder);
            return authenticationProvider;
          }
    
          @Bean
          public AuthenticationManager authenticationManager(AuthenticationConfiguration config) throws Exception {
            return config.getAuthenticationManager();
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/filters/"

    - fileType: "UserAuthController"
      fileName: "UserAuthController"
      extension: "java"
      content: |
        package ${groupLink}.${lowerCase(projectName)}.config.security.controllers;

        import java.util.List;
        import jakarta.validation.Valid;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.AuthRequest;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.PasswordChangeRequest;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.UserUpdateRequest;
        import ${groupLink}.${lowerCase(projectName)}.config.security.models.RefreshRequest;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.JwtService;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.TokenBlacklistService;
        import ${groupLink}.${lowerCase(projectName)}.config.security.services.UserInfoService;
        import ${groupLink}.${lowerCase(projectName)}.dto.RestResponse;
        import ${groupLink}.${lowerCase(projectName)}.models.RoleEntity;
        import ${groupLink}.${lowerCase(projectName)}.models.UserEntity;
        import ${groupLink}.${lowerCase(projectName)}.repositories.RoleEntityRepository;
        import org.springframework.beans.factory.annotation.Autowired;
        import org.springframework.http.HttpStatus;
        import org.springframework.http.ResponseEntity;
        import org.springframework.security.core.userdetails.UserDetails;
        import org.springframework.web.bind.annotation.*;
        
        import io.swagger.v3.oas.annotations.Operation;
        import io.swagger.v3.oas.annotations.media.ArraySchema;
        import io.swagger.v3.oas.annotations.media.Content;
        import io.swagger.v3.oas.annotations.media.Schema;
        import io.swagger.v3.oas.annotations.responses.ApiResponse;
        import io.swagger.v3.oas.annotations.responses.ApiResponses;
        import io.swagger.v3.oas.annotations.security.SecurityRequirement;
        import io.swagger.v3.oas.annotations.tags.Tag;
        
        @RestController
        @RequestMapping("/auth")
        @Tag(name = "Authentication", description = "Endpoints for user authentication and management")
        public class UserAuthController {
          private final JwtService jwtService;
          private final UserInfoService userInfoService;
          private final TokenBlacklistService tokenBlacklistService;
          @Autowired
          private RoleEntityRepository roleEntityRepository;
          
          @Autowired
          public UserAuthController(JwtService jwtService, UserInfoService userInfoService, TokenBlacklistService tokenBlacklistService) {
            this.jwtService = jwtService;
            this.userInfoService = userInfoService;
            this.tokenBlacklistService = tokenBlacklistService;
          }
          
          @Operation(
                summary = "Get all available roles",
                description = "Retrieve the complete list of roles available in the system. This endpoint is public and does not require authentication."
          )
          @ApiResponses({
                @ApiResponse(responseCode = "200", description = "Successfully retrieved roles list",
                        content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class), array = @ArraySchema(schema = @Schema(implementation = RoleEntity.class))
                        )
                ),
                @ApiResponse(responseCode = "500", description = "Internal server error",
                        content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
                )
          })
          @GetMapping("/roles")
          public ResponseEntity<RestResponse<List<RoleEntity>>> listRoles() {
            var roles = roleEntityRepository.findAll();
            return ResponseEntity.ok(RestResponse.buildSuccessResponse(HttpStatus.OK, "Roles", roles));
          }
          
          @Operation(
            security = { @SecurityRequirement(name = "bearerAuth") },
            summary = "Logout user",
            description = "Invalidate the user's JWT token"
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "Successfully logged out",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(
              responseCode = "400",
              description = "Invalid Authorization header",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
           )
          })
          @PostMapping("/logout")
          public ResponseEntity<RestResponse<String>> logout(@RequestHeader("Authorization") String authHeader) {
            if (authHeader != null && authHeader.startsWith("Bearer ")) {
              String token = authHeader.substring(7);
              tokenBlacklistService.invalidateToken(token);
              return ResponseEntity.ok(
                RestResponse.buildSuccessResponse(
                  HttpStatus.OK,
                  "Logged out successfully",
                  null
                )
              );
            }
            return ResponseEntity.badRequest().body(
              RestResponse.buildErrorResponse(
                HttpStatus.BAD_REQUEST,
                "Invalid Authorization Header",
                null
              )
            );
          }
        
          @Operation(
            security = { @SecurityRequirement(name = "bearerAuth") },
            summary = "Get current user",
            description = "Retrieve details of the currently authenticated user"
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "Successfully retrieved user details",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(
              responseCode = "400",
              description = "No valid user session",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
           )
          })
          @GetMapping("/current")
          public ResponseEntity<RestResponse<UserDetails>> currentUser(@RequestHeader("Authorization") String authHeader) {
            try {
              if (authHeader != null && authHeader.startsWith("Bearer ")) {
                String token = authHeader.substring(7);
                String username = jwtService.extractUsername(token);
                UserDetails userConnected = userInfoService.loadUserByUsername(username);
                return ResponseEntity.ok(
                  RestResponse.buildSuccessResponse(
                    HttpStatus.OK,
                    "User details retrieved",
                    userConnected
                  )
                );
              }
            } catch (Exception e) {
              return ResponseEntity.badRequest().body(
                RestResponse.buildErrorResponse(
                  HttpStatus.BAD_REQUEST,
                  "Error retrieving user: " + e.getMessage(),
                  null
                )
              );
            }
            return ResponseEntity.badRequest().body(
                RestResponse.buildErrorResponse(
                    HttpStatus.BAD_REQUEST,
                    "No user connected",
                    null
                )
            );
          }
        
          @Operation(
              summary = "Register new user",
              description = "Create a new user account"
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "User created successfully",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(
              responseCode = "400",
              description = "Invalid user data",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
           )
          })
          @PostMapping("/register")
          public ResponseEntity<RestResponse<String>> addNewUser(@RequestBody UserEntity user) {
            try {
              if (user.getRoleidRoleEntity() == null) {
                var role = roleEntityRepository.findAllByName("ROLE_ADMIN")
                .orElseThrow(() -> new IllegalArgumentException("Default role ROLE_ADMIN not found"));
                user.setRoleidRoleEntity(role);
              }
              String result = userInfoService.addUser(user);
              return ResponseEntity.ok(RestResponse.buildSuccessResponse(HttpStatus.OK, "User registered successfully", result));
            } catch (Exception e) {
              return ResponseEntity.badRequest().body(RestResponse.buildErrorResponse(HttpStatus.BAD_REQUEST, "Registration failed: " + e.getMessage(), null));
            }
          }

          @Operation(
              summary = "User login",
              description = "Authenticate user and generate JWT token"
          )
          @ApiResponses({
              @ApiResponse(
                  responseCode = "200",
                  description = "Authentication successful",
                  content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
              ),
              @ApiResponse(
                  responseCode = "401",
                  description = "Invalid credentials",
                  content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
              )
          })
          @PostMapping("/login")
          public ResponseEntity<RestResponse<String>> authenticateAndGetToken(@Valid @RequestBody AuthRequest authRequest) {
              try {
                  String token = userInfoService.authenticateAndGetToken(authRequest);
                  return ResponseEntity.ok(
                      RestResponse.buildSuccessResponse(
                          HttpStatus.OK,
                          "Authentication successful",
                          token
                      )
                  );
              } catch (Exception e) {
                  return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(
                      RestResponse.buildErrorResponse(
                          HttpStatus.UNAUTHORIZED,
                          "Authentication failed: " + e.getMessage(),
                          null
                      )
                  );
              }
          }
          
          @Operation(
              summary = "Refresh access token",
              description = "Provide the current access-token to obtain a new one; the old one is invalidated."
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "New token issued",
              content = @Content(schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(responseCode = "401", description = "Token invalid or expired")
          })
          @PostMapping("/refresh")
          public ResponseEntity<RestResponse<String>> refresh(
            @Valid @RequestBody RefreshRequest req) {
            try {
              String newToken = userInfoService.refreshAccessToken(req.refreshToken());
              tokenBlacklistService.invalidateToken(req.refreshToken());
              return ResponseEntity.ok(
                RestResponse.buildSuccessResponse(HttpStatus.OK, "Token refreshed", newToken)
              );
            } catch (Exception e) {
              return ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(
                RestResponse.buildErrorResponse(HttpStatus.UNAUTHORIZED, e.getMessage(), null)
              );
            }
          }
        
          @Operation(
            security = { @SecurityRequirement(name = "bearerAuth") },
            summary = "Update user information",
            description = "Update the current user's name and email"
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "User updated successfully",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(
              responseCode = "400",
              description = "Invalid data or update failed",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
           )
          })
          @PatchMapping("/update")
          public ResponseEntity<RestResponse<String>> updateUser(
          @RequestHeader("Authorization") String authHeader,
          @Valid @RequestBody UserUpdateRequest updateRequest) {
          
            try {
              if (authHeader != null && authHeader.startsWith("Bearer ")) {
                String token = authHeader.substring(7);
                String username = jwtService.extractUsername(token);
                userInfoService.updateUser(username, updateRequest);
          
                return ResponseEntity.ok(
                  RestResponse.buildSuccessResponse(
                    HttpStatus.OK,
                    "User updated successfully",
                    null
                  )
                );
              }
              return ResponseEntity.badRequest().body(
                RestResponse.buildErrorResponse(
                  HttpStatus.BAD_REQUEST,
                  "Invalid Authorization Header",
                  null
                )
              );
          } catch (Exception e) {
            return ResponseEntity.badRequest().body(
            RestResponse.buildErrorResponse(
            HttpStatus.BAD_REQUEST,
            "Update failed: " + e.getMessage(),
                          null
                      )
                  );
              }
          }
        
          @Operation(
            security = { @SecurityRequirement(name = "bearerAuth") },
            summary = "Change user password",
            description = "Change the current user's password. Requires old password, new password and confirmation."
          )
          @ApiResponses({
            @ApiResponse(
              responseCode = "200",
              description = "Password changed successfully",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
            ),
            @ApiResponse(
              responseCode = "400",
              description = "Invalid data or password change failed",
              content = @Content(mediaType = "application/json", schema = @Schema(implementation = RestResponse.class))
           )
          })
          @PostMapping("/change-password")
          public ResponseEntity<RestResponse<String>> changePassword(
          @RequestHeader("Authorization") String authHeader,
          @Valid @RequestBody PasswordChangeRequest passwordChangeRequest) {
          
            try {
              if (authHeader != null && authHeader.startsWith("Bearer ")) {
                String token = authHeader.substring(7); 
                String username = jwtService.extractUsername(token);
                userInfoService.changeUserPassword(
                  username,
                  passwordChangeRequest.getOldPassword(),
                  passwordChangeRequest.getNewPassword(),
                  passwordChangeRequest.getConfirmPassword()
                );
            
                return ResponseEntity.ok(
                  RestResponse.buildSuccessResponse(
                    HttpStatus.OK,
                    "Password changed successfully",
                    null
                  )
                );
              }
              return ResponseEntity.badRequest().body(
                RestResponse.buildErrorResponse(
                  HttpStatus.BAD_REQUEST,
                  "Invalid Authorization Header",
                  null
                )
              );
            } catch (Exception e) {
              return ResponseEntity.badRequest().body(
                RestResponse.buildErrorResponse(
                  HttpStatus.BAD_REQUEST,
                  "Password change failed: " + e.getMessage(),
                  null
                )
              );
            }
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/src/main/java/${groupLinkPath}/${lowerCase(projectName)}/config/security/controllers/"

  additionalDependencies:
    - groupId: "org.springframework.boot"
      artifactId: "spring-boot-starter-security"
      version: "{{removeLine}}"
      scope: "{{removeLine}}"
    - groupId: "io.jsonwebtoken"
      artifactId: "jjwt-api"
      version: "0.11.5"
      scope: "{{removeLine}}"
    - groupId: "io.jsonwebtoken"
      artifactId: "jjwt-impl"
      version: "0.11.5"
      scope: "{{removeLine}}"
    - groupId: "io.jsonwebtoken"
      artifactId: "jjwt-jackson"
      version: "0.11.5"
      scope: "{{removeLine}}"
  metadataBooleanTrueKeys:
    - "useBearerToken"
    - "useSpringSecurityJWT"
- id: 2
  frameworkId: 2
  name: ".NET Security - JWT"
  securityFiles:
    - fileType: "Init Script"
      fileName: "identity_schema"
      extension: "sql"
      content: |
        --Default login
        --Username: admin
        --Password: Pwd@123
        {{#if databaseType = PostgreSQL}}
          CREATE TABLE IF NOT EXISTS "AspNetRoles" (
            "Id" TEXT NOT NULL,
            "Name" VARCHAR(256),
            "NormalizedName" VARCHAR(256),
            "ConcurrencyStamp" TEXT,
            CONSTRAINT "PK_AspNetRoles" PRIMARY KEY ("Id"),
            CONSTRAINT "RoleNameIndex" UNIQUE ("NormalizedName")
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetUsers" (
            "Id" TEXT NOT NULL,
            "UserName" VARCHAR(256),
            "NormalizedUserName" VARCHAR(256),
            "Email" VARCHAR(256),
            "NormalizedEmail" VARCHAR(256),
            "EmailConfirmed" BOOLEAN NOT NULL DEFAULT FALSE,
            "PasswordHash" TEXT,
            "SecurityStamp" TEXT,
            "ConcurrencyStamp" TEXT,
            "PhoneNumber" TEXT,
            "PhoneNumberConfirmed" BOOLEAN NOT NULL DEFAULT FALSE,
            "TwoFactorEnabled" BOOLEAN NOT NULL DEFAULT FALSE,
            "LockoutEnd" TIMESTAMPTZ,
            "LockoutEnabled" BOOLEAN NOT NULL DEFAULT FALSE,
            "AccessFailedCount" INTEGER NOT NULL DEFAULT 0,
            CONSTRAINT "PK_AspNetUsers" PRIMARY KEY ("Id"),
            CONSTRAINT "UserNameIndex" UNIQUE ("NormalizedUserName")
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetRoleClaims" (
            "Id" SERIAL NOT NULL,
            "RoleId" TEXT NOT NULL,
            "ClaimType" TEXT,
            "ClaimValue" TEXT,
            CONSTRAINT "PK_AspNetRoleClaims" PRIMARY KEY ("Id"),
            CONSTRAINT "FK_AspNetRoleClaims_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetUserClaims" (
            "Id" SERIAL NOT NULL,
            "UserId" TEXT NOT NULL,
            "ClaimType" TEXT,
            "ClaimValue" TEXT,
            CONSTRAINT "PK_AspNetUserClaims" PRIMARY KEY ("Id"),
            CONSTRAINT "FK_AspNetUserClaims_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetUserLogins" (
            "LoginProvider" TEXT NOT NULL,
            "ProviderKey" TEXT NOT NULL,
            "ProviderDisplayName" TEXT,
            "UserId" TEXT NOT NULL,
            CONSTRAINT "PK_AspNetUserLogins" PRIMARY KEY ("LoginProvider", "ProviderKey"),
            CONSTRAINT "FK_AspNetUserLogins_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetUserRoles" (
            "UserId" TEXT NOT NULL,
            "RoleId" TEXT NOT NULL,
            CONSTRAINT "PK_AspNetUserRoles" PRIMARY KEY ("UserId", "RoleId"),
            CONSTRAINT "FK_AspNetUserRoles_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE,
            CONSTRAINT "FK_AspNetUserRoles_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
          
          CREATE TABLE IF NOT EXISTS "AspNetUserTokens" (
            "UserId" TEXT NOT NULL,
            "LoginProvider" TEXT NOT NULL,
            "Name" TEXT NOT NULL,
            "Value" TEXT,
            CONSTRAINT "PK_AspNetUserTokens" PRIMARY KEY ("UserId", "LoginProvider", "Name"),
            CONSTRAINT "FK_AspNetUserTokens_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          -- Seed data
          INSERT INTO "AspNetRoles" ("Id", "Name", "NormalizedName", "ConcurrencyStamp")
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', gen_random_uuid())
          ON CONFLICT ("Id") DO NOTHING;
          
          INSERT INTO "AspNetUsers" ("Id", "UserName", "NormalizedUserName", "Email", "NormalizedEmail", "EmailConfirmed", "PasswordHash", "SecurityStamp", "ConcurrencyStamp", "LockoutEnabled", "AccessFailedCount")
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', TRUE, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', gen_random_uuid(), gen_random_uuid(), FALSE, 0)
          ON CONFLICT ("Id") DO NOTHING;
          
          INSERT INTO "AspNetUserRoles" ("UserId", "RoleId")
          VALUES ('1', '1')
          ON CONFLICT ("UserId", "RoleId") DO NOTHING;
          {{else}}{{removeLine}}{{/if}}

        {{#if databaseType = MySQL}}
          CREATE TABLE IF NOT EXISTS AspNetRoles (
            Id VARCHAR(255) NOT NULL,
            Name VARCHAR(256),
            NormalizedName VARCHAR(256),
            ConcurrencyStamp TEXT,
            PRIMARY KEY (Id),
            UNIQUE INDEX RoleNameIndex (NormalizedName)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetUsers (
            Id VARCHAR(255) NOT NULL,
            UserName VARCHAR(256),
            NormalizedUserName VARCHAR(256),
            Email VARCHAR(256),
            NormalizedEmail VARCHAR(256),
            EmailConfirmed BOOLEAN NOT NULL DEFAULT FALSE,
            PasswordHash TEXT,
            SecurityStamp TEXT,
            ConcurrencyStamp TEXT,
            PhoneNumber TEXT,
            PhoneNumberConfirmed BOOLEAN NOT NULL DEFAULT FALSE,
            TwoFactorEnabled BOOLEAN NOT NULL DEFAULT FALSE,
            LockoutEnd DATETIME,
            LockoutEnabled BOOLEAN NOT NULL DEFAULT FALSE,
            AccessFailedCount INT NOT NULL DEFAULT 0,
            PRIMARY KEY (Id),
            UNIQUE INDEX UserNameIndex (NormalizedUserName),
            INDEX EmailIndex (NormalizedEmail)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetRoleClaims (
            Id INT AUTO_INCREMENT NOT NULL,
            RoleId VARCHAR(255) NOT NULL,
            ClaimType TEXT,
            ClaimValue TEXT,
            PRIMARY KEY (Id),
            FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE,
            INDEX IX_AspNetRoleClaims_RoleId (RoleId)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetUserClaims (
            Id INT AUTO_INCREMENT NOT NULL,
            UserId VARCHAR(255) NOT NULL,
            ClaimType TEXT,
            ClaimValue TEXT,
            PRIMARY KEY (Id),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserClaims_UserId (UserId)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetUserLogins (
            LoginProvider VARCHAR(255) NOT NULL,
            ProviderKey VARCHAR(255) NOT NULL,
            ProviderDisplayName TEXT,
            UserId VARCHAR(255) NOT NULL,
            PRIMARY KEY (LoginProvider, ProviderKey),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserLogins_UserId (UserId)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetUserRoles (
            UserId VARCHAR(255) NOT NULL,
            RoleId VARCHAR(255) NOT NULL,
            PRIMARY KEY (UserId, RoleId),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserRoles_RoleId (RoleId)
          ) ENGINE=InnoDB;
          
          CREATE TABLE IF NOT EXISTS AspNetUserTokens (
            UserId VARCHAR(255) NOT NULL,
            LoginProvider VARCHAR(255) NOT NULL,
            Name VARCHAR(255) NOT NULL,
            Value TEXT,
            PRIMARY KEY (UserId, LoginProvider, Name),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE
          ) ENGINE=InnoDB;
          
          -- Seed data
          INSERT INTO AspNetRoles (Id, Name, NormalizedName, ConcurrencyStamp)
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', UUID())
          ON DUPLICATE KEY UPDATE Id = Id;
          
          INSERT INTO AspNetUsers (Id, UserName, NormalizedUserName, Email, NormalizedEmail, EmailConfirmed, PasswordHash, SecurityStamp, ConcurrencyStamp, LockoutEnabled, AccessFailedCount)
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', TRUE, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', UUID(), UUID(), FALSE, 0)
          ON DUPLICATE KEY UPDATE Id = Id;
          
          INSERT INTO AspNetUserRoles (UserId, RoleId)
          VALUES ('1', '1')
          ON DUPLICATE KEY UPDATE UserId = UserId, RoleId = RoleId;
          {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = SQL Server}}
          CREATE TABLE [dbo].[AspNetRoles] (
            [Id] NVARCHAR(450) NOT NULL,
            [Name] NVARCHAR(256),
            [NormalizedName] NVARCHAR(256),
            [ConcurrencyStamp] NVARCHAR(MAX),
            CONSTRAINT [PK_AspNetRoles] PRIMARY KEY ([Id]),
            UNIQUE NONCLUSTERED ([NormalizedName])
          );
          
          CREATE TABLE [dbo].[AspNetUsers] (
          [Id] NVARCHAR(450) NOT NULL,
          [UserName] NVARCHAR(256),
          [NormalizedUserName] NVARCHAR(256),
          [Email] NVARCHAR(256),
          [NormalizedEmail] NVARCHAR(256),
          [EmailConfirmed] BIT NOT NULL DEFAULT 0,
          [PasswordHash] NVARCHAR(MAX),
          [SecurityStamp] NVARCHAR(MAX),
          [ConcurrencyStamp] NVARCHAR(MAX),
          [PhoneNumber] NVARCHAR(MAX),
          [PhoneNumberConfirmed] BIT NOT NULL DEFAULT 0,
          [TwoFactorEnabled] BIT NOT NULL DEFAULT 0,
          [LockoutEnd] DATETIMEOFFSET,
          [LockoutEnabled] BIT NOT NULL DEFAULT 0,
          [AccessFailedCount] INT NOT NULL DEFAULT 0,
          CONSTRAINT [PK_AspNetUsers] PRIMARY KEY ([Id]),
          UNIQUE NONCLUSTERED ([NormalizedUserName]),
          INDEX [EmailIndex] NONCLUSTERED ([NormalizedEmail])
          );
          
          CREATE TABLE [dbo].[AspNetRoleClaims] (
          [Id] INT IDENTITY(1,1) NOT NULL,
          [RoleId] NVARCHAR(450) NOT NULL,
          [ClaimType] NVARCHAR(MAX),
          [ClaimValue] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetRoleClaims] PRIMARY KEY ([Id]),
          CONSTRAINT [FK_AspNetRoleClaims_AspNetRoles_RoleId] FOREIGN KEY ([RoleId]) REFERENCES [dbo].[AspNetRoles]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetRoleClaims_RoleId] NONCLUSTERED ([RoleId])
          );
          
          CREATE TABLE [dbo].[AspNetUserClaims] (
          [Id] INT IDENTITY(1,1) NOT NULL,
          [UserId] NVARCHAR(450) NOT NULL,
          [ClaimType] NVARCHAR(MAX),
          [ClaimValue] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetUserClaims] PRIMARY KEY ([Id]),
          CONSTRAINT [FK_AspNetUserClaims_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserClaims_UserId] NONCLUSTERED ([UserId])
          );
          
          CREATE TABLE [dbo].[AspNetUserLogins] (
          [LoginProvider] NVARCHAR(450) NOT NULL,
          [ProviderKey] NVARCHAR(450) NOT NULL,
          [ProviderDisplayName] NVARCHAR(MAX),
          [UserId] NVARCHAR(450) NOT NULL,
          CONSTRAINT [PK_AspNetUserLogins] PRIMARY KEY ([LoginProvider], [ProviderKey]),
          CONSTRAINT [FK_AspNetUserLogins_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserLogins_UserId] NONCLUSTERED ([UserId])
          );
          
          CREATE TABLE [dbo].[AspNetUserRoles] (
          [UserId] NVARCHAR(450) NOT NULL,
          [RoleId] NVARCHAR(450) NOT NULL,
          CONSTRAINT [PK_AspNetUserRoles] PRIMARY KEY ([UserId], [RoleId]),
          CONSTRAINT [FK_AspNetUserRoles_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          CONSTRAINT [FK_AspNetUserRoles_AspNetRoles_RoleId] FOREIGN KEY ([RoleId]) REFERENCES [dbo].[AspNetRoles]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserRoles_RoleId] NONCLUSTERED ([RoleId])
          );
          
          CREATE TABLE [dbo].[AspNetUserTokens] (
          [UserId] NVARCHAR(450) NOT NULL,
          [LoginProvider] NVARCHAR(450) NOT NULL,
          [Name] NVARCHAR(450) NOT NULL,
          [Value] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetUserTokens] PRIMARY KEY ([UserId], [LoginProvider], [Name]),
          CONSTRAINT [FK_AspNetUserTokens_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE
          );
          
          -- Seed data
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetRoles] WHERE [Id] = '1')
          INSERT INTO [dbo].[AspNetRoles] ([Id], [Name], [NormalizedName], [ConcurrencyStamp])
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', NEWID());
          
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetUsers] WHERE [Id] = '1')
          INSERT INTO [dbo].[AspNetUsers] ([Id], [UserName], [NormalizedUserName], [Email], [NormalizedEmail], [EmailConfirmed], [PasswordHash], [SecurityStamp], [ConcurrencyStamp], [LockoutEnabled], [AccessFailedCount])
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', 1, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', NEWID(), NEWID(), 0, 0);
          
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetUserRoles] WHERE [UserId] = '1' AND [RoleId] = '1')
          INSERT INTO [dbo].[AspNetUserRoles] ([UserId], [RoleId])
          VALUES ('1', '1');
          {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = Oracle}}
          
          CREATE TABLE "AspNetRoles" (
          "Id" VARCHAR2(255) NOT NULL,
          "Name" VARCHAR2(256),
          "NormalizedName" VARCHAR2(256),
          "ConcurrencyStamp" VARCHAR2(4000),
          CONSTRAINT "PK_AspNetRoles" PRIMARY KEY ("Id"),
          CONSTRAINT "RoleNameIndex" UNIQUE ("NormalizedName")
          );
          
          CREATE TABLE "AspNetUsers" (
          "Id" VARCHAR2(255) NOT NULL,
          "UserName" VARCHAR2(256),
          "NormalizedUserName" VARCHAR2(256),
          "Email" VARCHAR2(256),
          "NormalizedEmail" VARCHAR2(256),
          "EmailConfirmed" NUMBER(1) NOT NULL,
          "PasswordHash" VARCHAR2(4000),
          "SecurityStamp" VARCHAR2(4000),
          "ConcurrencyStamp" VARCHAR2(4000),
          "PhoneNumber" VARCHAR2(4000),
          "PhoneNumberConfirmed" NUMBER(1) NOT NULL,
          "TwoFactorEnabled" NUMBER(1) NOT NULL,
          "LockoutEnd" TIMESTAMP,
          "LockoutEnabled" NUMBER(1) NOT NULL,
          "AccessFailedCount" NUMBER(10) NOT NULL,
          CONSTRAINT "PK_AspNetUsers" PRIMARY KEY ("Id"),
          CONSTRAINT "UserNameIndex" UNIQUE ("NormalizedUserName")
          );
          
          CREATE INDEX "EmailIndex" ON "AspNetUsers"("NormalizedEmail");
          
          CREATE TABLE "AspNetRoleClaims" (
          "Id" NUMBER(10) NOT NULL,
          "RoleId" VARCHAR2(255) NOT NULL,
          "ClaimType" VARCHAR2(4000),
          "ClaimValue" VARCHAR2(4000),
          CONSTRAINT "PK_AspNetRoleClaims" PRIMARY KEY ("Id"),
          CONSTRAINT "FK_AspNetRoleClaims_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
          
          CREATE SEQUENCE "AspNetRoleClaims_seq" START WITH 1 INCREMENT BY 1;
          
          CREATE OR REPLACE TRIGGER "AspNetRoleClaims_trigger"
          BEFORE INSERT ON "AspNetRoleClaims"
          FOR EACH ROW
          BEGIN
          SELECT "AspNetRoleClaims_seq".NEXTVAL INTO :new."Id" FROM dual;
          END;
          /
          
          CREATE INDEX "IX_AspNetRoleClaims_RoleId" ON "AspNetRoleClaims"("RoleId");
          
          CREATE TABLE "AspNetUserClaims" (
          "Id" NUMBER(10) NOT NULL,
          "UserId" VARCHAR2(255) NOT NULL,
          "ClaimType" VARCHAR2(4000),
          "ClaimValue" VARCHAR2(4000),
          CONSTRAINT "PK_AspNetUserClaims" PRIMARY KEY ("Id"),
          CONSTRAINT "FK_AspNetUserClaims_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          CREATE SEQUENCE "AspNetUserClaims_seq" START WITH 1 INCREMENT BY 1;
          
          CREATE OR REPLACE TRIGGER "AspNetUserClaims_trigger"
          BEFORE INSERT ON "AspNetUserClaims"
          FOR EACH ROW
          BEGIN
          SELECT "AspNetUserClaims_seq".NEXTVAL INTO :new."Id" FROM dual;
          END;
          /
          
          CREATE INDEX "IX_AspNetUserClaims_UserId" ON "AspNetUserClaims"("UserId");
          
          CREATE TABLE "AspNetUserLogins" (
          "LoginProvider" VARCHAR2(255) NOT NULL,
          "ProviderKey" VARCHAR2(255) NOT NULL,
          "ProviderDisplayName" VARCHAR2(4000),
          "UserId" VARCHAR2(255) NOT NULL,
          CONSTRAINT "PK_AspNetUserLogins" PRIMARY KEY ("LoginProvider", "ProviderKey"),
          CONSTRAINT "FK_AspNetUserLogins_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          CREATE INDEX "IX_AspNetUserLogins_UserId" ON "AspNetUserLogins"("UserId");
          
          CREATE TABLE "AspNetUserRoles" (
          "UserId" VARCHAR2(255) NOT NULL,
          "RoleId" VARCHAR2(255) NOT NULL,
          CONSTRAINT "PK_AspNetUserRoles" PRIMARY KEY ("UserId", "RoleId"),
          CONSTRAINT "FK_AspNetUserRoles_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE,
          CONSTRAINT "FK_AspNetUserRoles_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
          
          CREATE INDEX "IX_AspNetUserRoles_RoleId" ON "AspNetUserRoles"("RoleId");
          
          CREATE TABLE "AspNetUserTokens" (
          "UserId" VARCHAR2(255) NOT NULL,
          "LoginProvider" VARCHAR2(255) NOT NULL,
          "Name" VARCHAR2(255) NOT NULL,
          "Value" VARCHAR2(4000),
          CONSTRAINT "PK_AspNetUserTokens" PRIMARY KEY ("UserId", "LoginProvider", "Name"),
          CONSTRAINT "FK_AspNetUserTokens_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
          
          -- Données initiales
          MERGE INTO "AspNetRoles" r
          USING (SELECT '1' AS "Id", 'ROLE_ADMIN' AS "Name", 'ROLE_ADMIN' AS "NormalizedName", SYS_GUID() AS "ConcurrencyStamp" FROM dual) s
          ON (r."Id" = s."Id")
          WHEN NOT MATCHED THEN
          INSERT ("Id", "Name", "NormalizedName", "ConcurrencyStamp")
          VALUES (s."Id", s."Name", s."NormalizedName", s."ConcurrencyStamp");
          
          MERGE INTO "AspNetUsers" u
          USING (
          SELECT
          '1' AS "Id",
          'admin' AS "UserName",
          'ADMIN' AS "NormalizedUserName",
          'admin@user.com' AS "Email",
          'ADMIN@USER.COM' AS "NormalizedEmail",
          1 AS "EmailConfirmed",
          'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==' AS "PasswordHash",
          SYS_GUID() AS "SecurityStamp",
          SYS_GUID() AS "ConcurrencyStamp",
          0 AS "LockoutEnabled",
          0 AS "AccessFailedCount",
          0 AS "PhoneNumberConfirmed",
          0 AS "TwoFactorEnabled",
          NULL AS "PhoneNumber",
          NULL AS "LockoutEnd"
          FROM dual
          ) s
          ON (u."Id" = s."Id")
          WHEN NOT MATCHED THEN
          INSERT (
          "Id", "UserName", "NormalizedUserName", "Email", "NormalizedEmail", "EmailConfirmed",
          "PasswordHash", "SecurityStamp", "ConcurrencyStamp", "LockoutEnabled", "AccessFailedCount",
          "PhoneNumberConfirmed", "TwoFactorEnabled", "PhoneNumber", "LockoutEnd"
          )
          VALUES (
          s."Id", s."UserName", s."NormalizedUserName", s."Email", s."NormalizedEmail", s."EmailConfirmed",
          s."PasswordHash", s."SecurityStamp", s."ConcurrencyStamp", s."LockoutEnabled", s."AccessFailedCount",
          s."PhoneNumberConfirmed", s."TwoFactorEnabled", s."PhoneNumber", s."LockoutEnd"
          );
          
          MERGE INTO "AspNetUserRoles" ur
          USING (SELECT '1' AS "UserId", '1' AS "RoleId" FROM dual) s
          ON (ur."UserId" = s."UserId" AND ur."RoleId" = s."RoleId")
          WHEN NOT MATCHED THEN
          INSERT ("UserId", "RoleId")
          VALUES (s."UserId", s."RoleId");
          {{else}}{{removeLine}}{{/if}}
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Data"

    - fileType: "User Roles Static Class"
      fileName: "UserRoles"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models;
        public static class UserRoles
        {
            public const string Admin = "Admin";
            public const string User = "User";
            // Ajouter d'autres rôles si nécessaire
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Models"

    - fileType: "Application User Entity"
      fileName: "ApplicationUser"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models;
        using Microsoft.AspNetCore.Identity;

        public class ApplicationUser : IdentityUser
        {
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Models"

    - fileType: "Auth Request DTO"
      fileName: "AuthRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.DTOs;
        public class AuthRequest
        {
            public string Username { get; set; }
            public string Password { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/DTOs"

    - fileType: "Register Request DTO"
      fileName: "RegisterRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.DTOs;
        public class RegisterRequest
        {
            public string Username { get; set; }
            public string Email { get; set; }
            public string Password { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/DTOs"

    - fileType: "Password Change Request DTO"
      fileName: "PasswordChangeRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.DTOs;
        public class PasswordChangeRequest
        {
            public string OldPassword { get; set; }
            public string NewPassword { get; set; }
            public string ConfirmPassword { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/DTOs"

    - fileType: "User Update Request DTO"
      fileName: "UserUpdateRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.DTOs;
        public class UserUpdateRequest
        {
            public string Name { get; set; }
            public string Email { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/DTOs"

    - fileType: "JWT Service"
      fileName: "JwtService"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Implementation;
        using System.IdentityModel.Tokens.Jwt;
        using System.Security.Claims;
        using System.Text;
        using Microsoft.Extensions.Configuration;
        using Microsoft.IdentityModel.Tokens;
        
        public class JwtService
        {
          private readonly IConfiguration _config;
          public JwtService(IConfiguration config) => _config = config;
      
          public string GenerateToken(string username)
          {
            var claims = new[]
            {
              new Claim(JwtRegisteredClaimNames.Sub, username),
              new Claim(JwtRegisteredClaimNames.Jti, Guid.NewGuid().ToString())
            };
            
            var key = new SymmetricSecurityKey(Convert.FromBase64String(_config["Jwt:SecretKey"]));
            var creds = new SigningCredentials(key, SecurityAlgorithms.HmacSha256);
            
            var token = new JwtSecurityToken(
              claims: claims,
              expires: DateTime.UtcNow.AddMinutes(30),
              signingCredentials: creds
            );
            return new JwtSecurityTokenHandler().WriteToken(token);
          }
      
          public string ExtractUsername(string token)
          {
            var principal = new JwtSecurityTokenHandler()
            .ValidateToken(token, new TokenValidationParameters
            {
              ValidateIssuerSigningKey = true,
              IssuerSigningKey = new SymmetricSecurityKey(Convert.FromBase64String(_config["Jwt:SecretKey"])),
              ValidateIssuer = false,
              ValidateAudience = false,
              ClockSkew = TimeSpan.Zero
            }, out _);
            return principal.Identity.Name;
          }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Services/Implementation"

    - fileType: "Token Blacklist Service"
      fileName: "TokenBlacklistService"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Implementation;
        public class TokenBlacklistService
        {
            private readonly Dictionary<string, DateTime> _invalidatedTokens = new();

            public void InvalidateToken(string token, DateTime expiration)
            {
                _invalidatedTokens[token] = expiration;
            }

            public bool IsTokenInvalidated(string token)
            {
                return _invalidatedTokens.TryGetValue(token, out var exp) && exp > DateTime.UtcNow;
            }

            public void CleanExpiredTokens()
            {
                var now = DateTime.UtcNow;
                foreach (var key in _invalidatedTokens.Where(x => x.Value < now).Select(x => x.Key).ToList())
                {
                    _invalidatedTokens.Remove(key);
                }
            }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Services/Implementation"

    - fileType: "User Service"
      fileName: "UserService"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Implementation;

        using ${majStart(projectName)}.DTOs;
        using Microsoft.AspNetCore.Identity;      
        using ${majStart(projectName)}.Models;

        public class UserService
        {
            private readonly UserManager<ApplicationUser> _userManager;
            private readonly JwtService _jwtService;

            public UserService(UserManager<ApplicationUser> userManager, JwtService jwtService)
            {
                _userManager = userManager;
                _jwtService = jwtService;
            }

            public async Task<string> Authenticate(AuthRequest request)
            {
                var user = await _userManager.FindByNameAsync(request.Username);
                if (user != null && await _userManager.CheckPasswordAsync(user, request.Password))
                {
                    return _jwtService.GenerateToken(user.UserName);
                }
                throw new UnauthorizedAccessException("Invalid credentials");
            }

            public async Task<string> Register(RegisterRequest request)
            {
                var user = new ApplicationUser
                {
                    UserName = request.Username,
                    Email = request.Email
                };

                var result = await _userManager.CreateAsync(user, request.Password);
                if (!result.Succeeded)
                    throw new Exception(string.Join(", ", result.Errors.Select(e => e.Description)));
                return user.Id;
            }


            public async Task ChangePassword(string username, PasswordChangeRequest model)
            {
                var user = await _userManager.FindByNameAsync(username);
                if (user == null || !await _userManager.CheckPasswordAsync(user, model.OldPassword))
                    throw new Exception("Invalid current password");

                if (model.NewPassword != model.ConfirmPassword)
                    throw new Exception("Passwords do not match");

                await _userManager.ChangePasswordAsync(user, model.OldPassword, model.NewPassword);
            }

            public async Task UpdateUser(string username, UserUpdateRequest update)
            {
                var user = await _userManager.FindByNameAsync(username);
                if (user == null) throw new Exception("User not found");
                user.Email = update.Email;
                user.UserName = update.Name;
                await _userManager.UpdateAsync(user);
            }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Services/Implementation"

    - fileType: "Authentication Controller"
      fileName: "AuthController"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Controllers;

        using Microsoft.AspNetCore.Authorization;
        using Microsoft.AspNetCore.Mvc;
        using Microsoft.AspNetCore.Http;
        using ${majStart(projectName)}.DTOs;
        using ${majStart(projectName)}.Utils;
        using ${majStart(projectName)}.Services.Implementation;
        using Swashbuckle.AspNetCore.Annotations;

        [ApiController]
        [Route("auth")]
        [Produces("application/json")]
        [Consumes("application/json")]
        [ApiExplorerSettings(GroupName = "v1")]
        [SwaggerTag("Authentication APIs")]
        public class AuthController : ControllerBase
        {
            private readonly UserService _userService;

            public AuthController(UserService userService)
            {
                _userService = userService;
            }

            [HttpPost("login")]
            [AllowAnonymous]
            [SwaggerOperation(Summary = "Login", Description = "Authenticate user and return a JWT token")]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status200OK)]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status500InternalServerError)]
            public async Task<IActionResult> Login([FromBody] AuthRequest request)
            {
                try
                {
                    var token = await _userService.Authenticate(request);
                    return Ok(new ApiResponse<string>
                    {
                        Status = StatusCodes.Status200OK,
                        Message = "You are logged in",
                        ReturnCode = 1,
                        Data = token
                    });
                }
                catch (Exception e)
                {
                    return StatusCode(StatusCodes.Status500InternalServerError, new ApiResponse<string>
                    {
                        Status = StatusCodes.Status500InternalServerError,
                        Message = e.Message,
                        ReturnCode = 0,
                        Data = null
                    });
                }
            }

            [HttpPost("register")]
            [AllowAnonymous]
            [SwaggerOperation(Summary = "Register", Description = "Register a new user")]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status201Created)]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status500InternalServerError)]
            public async Task<IActionResult> Register([FromBody] RegisterRequest request)
            {
                try
                {
                    var id = await _userService.Register(request);
                    return StatusCode(StatusCodes.Status201Created, new ApiResponse<string>
                    {
                        Status = StatusCodes.Status201Created,
                        Message = "User created successfully",
                        ReturnCode = 1,
                        Data = id
                    });
                }
                catch (Exception e)
                {
                    return StatusCode(StatusCodes.Status500InternalServerError, new ApiResponse<string>
                    {
                        Status = StatusCodes.Status500InternalServerError,
                        Message = e.Message,
                        ReturnCode = 0,
                        Data = null
                    });
                }
            }

            [HttpPost("change-password")]
            [Authorize]
            [SwaggerOperation(Summary = "Change Password", Description = "Change the password of the logged-in user")]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status200OK)]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status500InternalServerError)]
            public async Task<IActionResult> ChangePassword([FromBody] PasswordChangeRequest model)
            {
                try
                {
                    var username = User.Identity?.Name;
                    await _userService.ChangePassword(username, model);
                    return Ok(new ApiResponse<string>
                    {
                        Status = StatusCodes.Status200OK,
                        Message = "Password changed successfully",
                        ReturnCode = 1
                    });
                }
                catch (Exception e)
                {
                    return StatusCode(StatusCodes.Status500InternalServerError, new ApiResponse<string>
                    {
                        Status = StatusCodes.Status500InternalServerError,
                        Message = e.Message,
                        ReturnCode = 0
                    });
                }
            }

            [HttpPatch("update")]
            [Authorize]
            [SwaggerOperation(Summary = "Update User", Description = "Update user information of the logged-in user")]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status200OK)]
            [ProducesResponseType(typeof(ApiResponse<string>), StatusCodes.Status500InternalServerError)]
            public async Task<IActionResult> Update([FromBody] UserUpdateRequest model)
            {
                try
                {
                    var username = User.Identity?.Name;
                    await _userService.UpdateUser(username, model);
                    return Ok(new ApiResponse<string>
                    {
                        Status = StatusCodes.Status200OK,
                        Message = "User updated successfully",
                        ReturnCode = 1
                    });
                }
                catch (Exception e)
                {
                    return StatusCode(StatusCodes.Status500InternalServerError, new ApiResponse<string>
                    {
                        Status = StatusCodes.Status500InternalServerError,
                        Message = e.Message,
                        ReturnCode = 0
                    });
                }
            }
        }

      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Controllers"

    - fileType: "SQL Schema Bootstrapper Service"
      fileName: "SqlSchemaBootstrapper"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Startup;

        using Microsoft.Extensions.Hosting;
        using Microsoft.Extensions.DependencyInjection;
        using Microsoft.EntityFrameworkCore;
        using System.Data.Common;
        using ${majStart(projectName)}.Data;
    
        public class SqlSchemaBootstrapper : IHostedService
        {
            private readonly IServiceProvider _provider;
            private const string SchemaFilePath = "Data/identity_schema.sql"; // chemin du script SQL

            public SqlSchemaBootstrapper(IServiceProvider provider)
            {
                _provider = provider;
            }

            public async Task StartAsync(CancellationToken cancellationToken)
            {
                using var scope = _provider.CreateScope();
                var context = scope.ServiceProvider.GetRequiredService<${majStart(projectName)}Context>();
                var db = context.Database;
                var conn = db.GetDbConnection();

                try
                {
                    await conn.OpenAsync(cancellationToken);

                    if (!await TableExistsAsync(conn, "AspNetUsers"))
                    {
                        var sql = await File.ReadAllTextAsync(SchemaFilePath, cancellationToken);
                        using var command = conn.CreateCommand();
                        command.CommandText = sql;
                        await command.ExecuteNonQueryAsync(cancellationToken);

                        Console.WriteLine("[Identity Schema] Tables créées automatiquement.");
                    }
                    else
                    {
                        Console.WriteLine("[Identity Schema] Tables déjà existantes.");
                    }
                }
                catch (Exception ex)
                {
                    Console.WriteLine($"[Identity Schema] ERREUR : {ex.Message}");
                }
                finally
                {
                    await conn.CloseAsync();
                }
            }

            public Task StopAsync(CancellationToken cancellationToken) => Task.CompletedTask;

            private async Task<bool> TableExistsAsync(DbConnection connection, string tableName)
            {
                string query = connection.GetType().Name switch
                {
                    var n when n.Contains("Npgsql") => $"SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = '{tableName.ToLower()}');",
                    var n when n.Contains("MySql") => $"SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '{tableName}';",
                    var n when n.Contains("SqlConnection") => $"SELECT COUNT(*) FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = '{tableName}';",
                    var n when n.Contains("Oracle") => $"SELECT COUNT(*) FROM user_tables WHERE table_name = UPPER('{tableName}');",
                    _ => throw new NotSupportedException($"Type de connexion non reconnu : {connection.GetType().Name}")
                };

                using var cmd = connection.CreateCommand();
                cmd.CommandText = query;
                var result = await cmd.ExecuteScalarAsync();
                return Convert.ToInt32(result) > 0;
            }
        
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/${majStart(projectName)}/Services/Startup"

  additionalDependencies: [ ]
  metadataBooleanTrueKeys:
    - "useJWT"
- id: 3
  frameworkId: 5
  name: ".NET Security - Cookie Authentication"
  securityFiles:
    - fileType: "Init Script"
      fileName: "identity_schema"
      extension: "sql"
      content: |
        -- Default login
        -- Username: admin
        -- Password: Pwd@123
        {{#if databaseType = PostgreSQL}}
          CREATE TABLE IF NOT EXISTS "AspNetRoles" (
            "Id" TEXT NOT NULL,
            "Name" VARCHAR(256),
            "NormalizedName" VARCHAR(256),
            "ConcurrencyStamp" TEXT,
            CONSTRAINT "PK_AspNetRoles" PRIMARY KEY ("Id"),
            CONSTRAINT "RoleNameIndex" UNIQUE ("NormalizedName")
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetUsers" (
            "Id" TEXT NOT NULL,
            "UserName" VARCHAR(256),
            "NormalizedUserName" VARCHAR(256),
            "Email" VARCHAR(256),
            "NormalizedEmail" VARCHAR(256),
            "EmailConfirmed" BOOLEAN NOT NULL DEFAULT FALSE,
            "PasswordHash" TEXT,
            "SecurityStamp" TEXT,
            "ConcurrencyStamp" TEXT,
            "PhoneNumber" TEXT,
            "PhoneNumberConfirmed" BOOLEAN NOT NULL DEFAULT FALSE,
            "TwoFactorEnabled" BOOLEAN NOT NULL DEFAULT FALSE,
            "LockoutEnd" TIMESTAMPTZ,
            "LockoutEnabled" BOOLEAN NOT NULL DEFAULT FALSE,
            "AccessFailedCount" INTEGER NOT NULL DEFAULT 0,
            CONSTRAINT "PK_AspNetUsers" PRIMARY KEY ("Id"),
            CONSTRAINT "UserNameIndex" UNIQUE ("NormalizedUserName")
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetRoleClaims" (
            "Id" SERIAL NOT NULL,
            "RoleId" TEXT NOT NULL,
            "ClaimType" TEXT,
            "ClaimValue" TEXT,
            CONSTRAINT "PK_AspNetRoleClaims" PRIMARY KEY ("Id"),
            CONSTRAINT "FK_AspNetRoleClaims_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetUserClaims" (
            "Id" SERIAL NOT NULL,
            "UserId" TEXT NOT NULL,
            "ClaimType" TEXT,
            "ClaimValue" TEXT,
            CONSTRAINT "PK_AspNetUserClaims" PRIMARY KEY ("Id"),
            CONSTRAINT "FK_AspNetUserClaims_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetUserLogins" (
            "LoginProvider" TEXT NOT NULL,
            "ProviderKey" TEXT NOT NULL,
            "ProviderDisplayName" TEXT,
            "UserId" TEXT NOT NULL,
            CONSTRAINT "PK_AspNetUserLogins" PRIMARY KEY ("LoginProvider", "ProviderKey"),
            CONSTRAINT "FK_AspNetUserLogins_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetUserRoles" (
            "UserId" TEXT NOT NULL,
            "RoleId" TEXT NOT NULL,
            CONSTRAINT "PK_AspNetUserRoles" PRIMARY KEY ("UserId", "RoleId"),
            CONSTRAINT "FK_AspNetUserRoles_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE,
            CONSTRAINT "FK_AspNetUserRoles_AspNetRoles_RoleId" FOREIGN KEY ("RoleId") REFERENCES "AspNetRoles"("Id") ON DELETE CASCADE
          );
        
          CREATE TABLE IF NOT EXISTS "AspNetUserTokens" (
            "UserId" TEXT NOT NULL,
            "LoginProvider" TEXT NOT NULL,
            "Name" TEXT NOT NULL,
            "Value" TEXT,
            CONSTRAINT "PK_AspNetUserTokens" PRIMARY KEY ("UserId", "LoginProvider", "Name"),
            CONSTRAINT "FK_AspNetUserTokens_AspNetUsers_UserId" FOREIGN KEY ("UserId") REFERENCES "AspNetUsers"("Id") ON DELETE CASCADE
          );
        
          -- Seed data
          INSERT INTO "AspNetRoles" ("Id", "Name", "NormalizedName", "ConcurrencyStamp")
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', gen_random_uuid())
          ON CONFLICT ("Id") DO NOTHING;
        
          INSERT INTO "AspNetUsers" ("Id", "UserName", "NormalizedUserName", "Email", "NormalizedEmail", "EmailConfirmed", "PasswordHash", "SecurityStamp", "ConcurrencyStamp", "LockoutEnabled", "AccessFailedCount")
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', TRUE, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', gen_random_uuid(), gen_random_uuid(), FALSE, 0)
          ON CONFLICT ("Id") DO NOTHING;
        
          INSERT INTO "AspNetUserRoles" ("UserId", "RoleId")
          VALUES ('1', '1')
          ON CONFLICT ("UserId", "RoleId") DO NOTHING;
          {{else}}{{removeLine}}{{/if}}

        {{#if databaseType = MySQL}}
          CREATE TABLE IF NOT EXISTS AspNetRoles (
            Id VARCHAR(255) NOT NULL,
            Name VARCHAR(256),
            NormalizedName VARCHAR(256),
            ConcurrencyStamp TEXT,
            PRIMARY KEY (Id),
            UNIQUE INDEX RoleNameIndex (NormalizedName)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetUsers (
            Id VARCHAR(255) NOT NULL,
            UserName VARCHAR(256),
            NormalizedUserName VARCHAR(256),
            Email VARCHAR(256),
            NormalizedEmail VARCHAR(256),
            EmailConfirmed BOOLEAN NOT NULL DEFAULT FALSE,
            PasswordHash TEXT,
            SecurityStamp TEXT,
            ConcurrencyStamp TEXT,
            PhoneNumber TEXT,
            PhoneNumberConfirmed BOOLEAN NOT NULL DEFAULT FALSE,
            TwoFactorEnabled BOOLEAN NOT NULL DEFAULT FALSE,
            LockoutEnd DATETIME,
            LockoutEnabled BOOLEAN NOT NULL DEFAULT FALSE,
            AccessFailedCount INT NOT NULL DEFAULT 0,
            PRIMARY KEY (Id),
            UNIQUE INDEX UserNameIndex (NormalizedUserName),
            INDEX EmailIndex (NormalizedEmail)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetRoleClaims (
            Id INT AUTO_INCREMENT NOT NULL,
            RoleId VARCHAR(255) NOT NULL,
            ClaimType TEXT,
            ClaimValue TEXT,
            PRIMARY KEY (Id),
            FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE,
            INDEX IX_AspNetRoleClaims_RoleId (RoleId)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetUserClaims (
            Id INT AUTO_INCREMENT NOT NULL,
            UserId VARCHAR(255) NOT NULL,
            ClaimType TEXT,
            ClaimValue TEXT,
            PRIMARY KEY (Id),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserClaims_UserId (UserId)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetUserLogins (
            LoginProvider VARCHAR(255) NOT NULL,
            ProviderKey VARCHAR(255) NOT NULL,
            ProviderDisplayName TEXT,
            UserId VARCHAR(255) NOT NULL,
            PRIMARY KEY (LoginProvider, ProviderKey),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserLogins_UserId (UserId)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetUserRoles (
            UserId VARCHAR(255) NOT NULL,
            RoleId VARCHAR(255) NOT NULL,
            PRIMARY KEY (UserId, RoleId),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE,
            INDEX IX_AspNetUserRoles_RoleId (RoleId)
          ) ENGINE=InnoDB;
        
          CREATE TABLE IF NOT EXISTS AspNetUserTokens (
            UserId VARCHAR(255) NOT NULL,
            LoginProvider VARCHAR(255) NOT NULL,
            Name VARCHAR(255) NOT NULL,
            Value TEXT,
            PRIMARY KEY (UserId, LoginProvider, Name),
            FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE
          ) ENGINE=InnoDB;
        
          -- Seed data
          INSERT INTO AspNetRoles (Id, Name, NormalizedName, ConcurrencyStamp)
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', UUID())
          ON DUPLICATE KEY UPDATE Id = Id;
        
          INSERT INTO AspNetUsers (Id, UserName, NormalizedUserName, Email, NormalizedEmail, EmailConfirmed, PasswordHash, SecurityStamp, ConcurrencyStamp, LockoutEnabled, AccessFailedCount)
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', TRUE, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', UUID(), UUID(), FALSE, 0)
          ON DUPLICATE KEY UPDATE Id = Id;
        
          INSERT INTO AspNetUserRoles (UserId, RoleId)
          VALUES ('1', '1')
          ON DUPLICATE KEY UPDATE UserId = UserId, RoleId = RoleId;
          {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = SQL Server}}
          CREATE TABLE [dbo].[AspNetRoles] (
            [Id] NVARCHAR(450) NOT NULL,
            [Name] NVARCHAR(256),
            [NormalizedName] NVARCHAR(256),
            [ConcurrencyStamp] NVARCHAR(MAX),
            CONSTRAINT [PK_AspNetRoles] PRIMARY KEY ([Id]),
            UNIQUE NONCLUSTERED ([NormalizedName])
          );
        
          CREATE TABLE [dbo].[AspNetUsers] (
          [Id] NVARCHAR(450) NOT NULL,
          [UserName] NVARCHAR(256),
          [NormalizedUserName] NVARCHAR(256),
          [Email] NVARCHAR(256),
          [NormalizedEmail] NVARCHAR(256),
          [EmailConfirmed] BIT NOT NULL DEFAULT 0,
          [PasswordHash] NVARCHAR(MAX),
          [SecurityStamp] NVARCHAR(MAX),
          [ConcurrencyStamp] NVARCHAR(MAX),
          [PhoneNumber] NVARCHAR(MAX),
          [PhoneNumberConfirmed] BIT NOT NULL DEFAULT 0,
          [TwoFactorEnabled] BIT NOT NULL DEFAULT 0,
          [LockoutEnd] DATETIMEOFFSET,
          [LockoutEnabled] BIT NOT NULL DEFAULT 0,
          [AccessFailedCount] INT NOT NULL DEFAULT 0,
          CONSTRAINT [PK_AspNetUsers] PRIMARY KEY ([Id]),
          UNIQUE NONCLUSTERED ([NormalizedUserName]),
          INDEX [EmailIndex] NONCLUSTERED ([NormalizedEmail])
          );
        
          CREATE TABLE [dbo].[AspNetRoleClaims] (
          [Id] INT IDENTITY(1,1) NOT NULL,
          [RoleId] NVARCHAR(450) NOT NULL,
          [ClaimType] NVARCHAR(MAX),
          [ClaimValue] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetRoleClaims] PRIMARY KEY ([Id]),
          CONSTRAINT [FK_AspNetRoleClaims_AspNetRoles_RoleId] FOREIGN KEY ([RoleId]) REFERENCES [dbo].[AspNetRoles]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetRoleClaims_RoleId] NONCLUSTERED ([RoleId])
          );
        
          CREATE TABLE [dbo].[AspNetUserClaims] (
          [Id] INT IDENTITY(1,1) NOT NULL,
          [UserId] NVARCHAR(450) NOT NULL,
          [ClaimType] NVARCHAR(MAX),
          [ClaimValue] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetUserClaims] PRIMARY KEY ([Id]),
          CONSTRAINT [FK_AspNetUserClaims_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserClaims_UserId] NONCLUSTERED ([UserId])
          );
        
          CREATE TABLE [dbo].[AspNetUserLogins] (
          [LoginProvider] NVARCHAR(450) NOT NULL,
          [ProviderKey] NVARCHAR(450) NOT NULL,
          [ProviderDisplayName] NVARCHAR(MAX),
          [UserId] NVARCHAR(450) NOT NULL,
          CONSTRAINT [PK_AspNetUserLogins] PRIMARY KEY ([LoginProvider], [ProviderKey]),
          CONSTRAINT [FK_AspNetUserLogins_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserLogins_UserId] NONCLUSTERED ([UserId])
          );
        
          CREATE TABLE [dbo].[AspNetUserRoles] (
          [UserId] NVARCHAR(450) NOT NULL,
          [RoleId] NVARCHAR(450) NOT NULL,
          CONSTRAINT [PK_AspNetUserRoles] PRIMARY KEY ([UserId], [RoleId]),
          CONSTRAINT [FK_AspNetUserRoles_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE,
          CONSTRAINT [FK_AspNetUserRoles_AspNetRoles_RoleId] FOREIGN KEY ([RoleId]) REFERENCES [dbo].[AspNetRoles]([Id]) ON DELETE CASCADE,
          INDEX [IX_AspNetUserRoles_RoleId] NONCLUSTERED ([RoleId])
          );
        
          CREATE TABLE [dbo].[AspNetUserTokens] (
          [UserId] NVARCHAR(450) NOT NULL,
          [LoginProvider] NVARCHAR(450) NOT NULL,
          [Name] NVARCHAR(450) NOT NULL,
          [Value] NVARCHAR(MAX),
          CONSTRAINT [PK_AspNetUserTokens] PRIMARY KEY ([UserId], [LoginProvider], [Name]),
          CONSTRAINT [FK_AspNetUserTokens_AspNetUsers_UserId] FOREIGN KEY ([UserId]) REFERENCES [dbo].[AspNetUsers]([Id]) ON DELETE CASCADE
          );
        
          -- Seed data
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetRoles] WHERE [Id] = '1')
          INSERT INTO [dbo].[AspNetRoles] ([Id], [Name], [NormalizedName], [ConcurrencyStamp])
          VALUES ('1', 'ROLE_ADMIN', 'ROLE_ADMIN', NEWID());
        
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetUsers] WHERE [Id] = '1')
          INSERT INTO [dbo].[AspNetUsers] ([Id], [UserName], [NormalizedUserName], [Email], [NormalizedEmail], [EmailConfirmed], [PasswordHash], [SecurityStamp], [ConcurrencyStamp], [LockoutEnabled], [AccessFailedCount])
          VALUES ('1', 'admin', 'ADMIN', 'admin@user.com', 'ADMIN@USER.COM', 1, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==', NEWID(), NEWID(), 0, 0);
        
          IF NOT EXISTS (SELECT 1 FROM [dbo].[AspNetUserRoles] WHERE [UserId] = '1' AND [RoleId] = '1')
          INSERT INTO [dbo].[AspNetUserRoles] ([UserId], [RoleId])
          VALUES ('1', '1');
          {{else}}{{removeLine}}{{/if}}
        
        {{#if databaseType = Oracle}}
          BEGIN
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetRoles (
            Id VARCHAR2(255) NOT NULL,
            Name VARCHAR2(256),
            NormalizedName VARCHAR2(256),
            ConcurrencyStamp VARCHAR2(4000),
            CONSTRAINT PK_AspNetRoles PRIMARY KEY (Id),
            CONSTRAINT RoleNameIndex UNIQUE (NormalizedName)
            )';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetUsers (
            Id VARCHAR2(255) NOT NULL,
            UserName VARCHAR2(256),
            NormalizedUserName VARCHAR2(256),
            Email VARCHAR2(256),
            NormalizedEmail VARCHAR2(256),
            EmailConfirmed NUMBER(1) NOT NULL DEFAULT 0,
            PasswordHash VARCHAR2(4000),
            SecurityStamp VARCHAR2(4000),
            ConcurrencyStamp VARCHAR2(4000),
            PhoneNumber VARCHAR2(4000),
            PhoneNumberConfirmed NUMBER(1) NOT NULL DEFAULT 0,
            TwoFactorEnabled NUMBER(1) NOT NULL DEFAULT 0,
            LockoutEnd TIMESTAMP,
            LockoutEnabled NUMBER(1) NOT NULL DEFAULT 0,
            AccessFailedCount NUMBER(10) NOT NULL DEFAULT 0,
            CONSTRAINT PK_AspNetUsers PRIMARY KEY (Id),
            CONSTRAINT UserNameIndex UNIQUE (NormalizedUserName)
            )';
        
            EXECUTE IMMEDIATE '
            CREATE INDEX EmailIndex ON AspNetUsers(NormalizedEmail)';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetRoleClaims (
            Id NUMBER(10) NOT NULL,
            RoleId VARCHAR2(255) NOT NULL,
            ClaimType VARCHAR2(4000),
            ClaimValue VARCHAR2(4000),
            CONSTRAINT PK_AspNetRoleClaims PRIMARY KEY (Id),
            CONSTRAINT FK_AspNetRoleClaims_AspNetRoles_RoleId FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE
            )';
        
            EXECUTE IMMEDIATE '
            CREATE SEQUENCE AspNetRoleClaims_seq START WITH 1 INCREMENT BY 1';
        
            EXECUTE IMMEDIATE '
            CREATE OR REPLACE TRIGGER AspNetRoleClaims_trigger
            BEFORE INSERT ON AspNetRoleClaims
            FOR EACH ROW
            BEGIN
            SELECT AspNetRoleClaims_seq.NEXTVAL INTO :new.Id FROM dual;
            END;';
        
            EXECUTE IMMEDIATE '
            CREATE INDEX IX_AspNetRoleClaims_RoleId ON AspNetRoleClaims(RoleId)';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetUserClaims (
            Id NUMBER(10) NOT NULL,
            UserId VARCHAR2(255) NOT NULL,
            ClaimType VARCHAR2(4000),
            ClaimValue VARCHAR2(4000),
            CONSTRAINT PK_AspNetUserClaims PRIMARY KEY (Id),
            CONSTRAINT FK_AspNetUserClaims_AspNetUsers_UserId FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE
            )';
        
            EXECUTE IMMEDIATE '
            CREATE SEQUENCE AspNetUserClaims_seq START WITH 1 INCREMENT BY 1';
        
            EXECUTE IMMEDIATE '
            CREATE OR REPLACE TRIGGER AspNetUserClaims_trigger
            BEFORE INSERT ON AspNetUserClaims
            FOR EACH ROW
            BEGIN
            SELECT AspNetUserClaims_seq.NEXTVAL INTO :new.Id FROM dual;
            END;';
        
            EXECUTE IMMEDIATE '
            CREATE INDEX IX_AspNetUserClaims_UserId ON AspNetUserClaims(UserId)';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetUserLogins (
            LoginProvider VARCHAR2(255) NOT NULL,
            ProviderKey VARCHAR2(255) NOT NULL,
            ProviderDisplayName VARCHAR2(4000),
            UserId VARCHAR2(255) NOT NULL,
            CONSTRAINT PK_AspNetUserLogins PRIMARY KEY (LoginProvider, ProviderKey),
            CONSTRAINT FK_AspNetUserLogins_AspNetUsers_UserId FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE
            )';
        
            EXECUTE IMMEDIATE '
            CREATE INDEX IX_AspNetUserLogins_UserId ON AspNetUserLogins(UserId)';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetUserRoles (
            UserId VARCHAR2(255) NOT NULL,
            RoleId VARCHAR2(255) NOT NULL,
            CONSTRAINT PK_AspNetUserRoles PRIMARY KEY (UserId, RoleId),
            CONSTRAINT FK_AspNetUserRoles_AspNetUsers_UserId FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE,
            CONSTRAINT FK_AspNetUserRoles_AspNetRoles_RoleId FOREIGN KEY (RoleId) REFERENCES AspNetRoles(Id) ON DELETE CASCADE
            )';
        
            EXECUTE IMMEDIATE '
            CREATE INDEX IX_AspNetUserRoles_RoleId ON AspNetUserRoles(RoleId)';
        
            EXECUTE IMMEDIATE '
            CREATE TABLE AspNetUserTokens (
            UserId VARCHAR2(255) NOT NULL,
            LoginProvider VARCHAR2(255) NOT NULL,
            Name VARCHAR2(255) NOT NULL,
            Value VARCHAR2(4000),
            CONSTRAINT PK_AspNetUserTokens PRIMARY KEY (UserId, LoginProvider, Name),
            CONSTRAINT FK_AspNetUserTokens_AspNetUsers_UserId FOREIGN KEY (UserId) REFERENCES AspNetUsers(Id) ON DELETE CASCADE
            )';
        
            -- Seed data
            EXECUTE IMMEDIATE '
            MERGE INTO AspNetRoles r
            USING (SELECT ''1'' AS Id, ''ROLE_ADMIN'' AS Name, ''ROLE_ADMIN'' AS NormalizedName, SYS_GUID() AS ConcurrencyStamp FROM dual) s
            ON (r.Id = s.Id)
            WHEN NOT MATCHED THEN
            INSERT (Id, Name, NormalizedName, ConcurrencyStamp)
            VALUES (s.Id, s.Name, s.NormalizedName, s.ConcurrencyStamp)';
        
            EXECUTE IMMEDIATE '
            MERGE INTO AspNetUsers u
            USING (SELECT ''1'' AS Id, ''admin'' AS UserName, ''ADMIN'' AS NormalizedUserName, ''admin@user.com'' AS Email, ''ADMIN@USER.COM'' AS NormalizedEmail,
            1 AS EmailConfirmed, 'AQAAAAIAAYagAAAAEI66CZE19cG5eCJ/Dcm/OF8zaVr89u8YZVsZdDYvVfLX/uN/GLqJKqNkmaQD9Cth4g==' AS PasswordHash,
            SYS_GUID() AS SecurityStamp, SYS_GUID() AS ConcurrencyStamp, 0 AS LockoutEnabled, 0 AS AccessFailedCount FROM dual) s
            ON (u.Id = s.Id)
            WHEN NOT MATCHED THEN
            INSERT (Id, UserName, NormalizedUserName, Email, NormalizedEmail, EmailConfirmed, PasswordHash, SecurityStamp, ConcurrencyStamp, LockoutEnabled, AccessFailedCount)
            VALUES (s.Id, s.UserName, s.NormalizedUserName, s.Email, s.NormalizedEmail, s.EmailConfirmed, s.PasswordHash, s.SecurityStamp, s.ConcurrencyStamp, s.LockoutEnabled, s.AccessFailedCount)';
        
            EXECUTE IMMEDIATE '
            MERGE INTO AspNetUserRoles ur
            USING (SELECT ''1'' AS UserId, ''1'' AS RoleId FROM dual) s
            ON (ur.UserId = s.UserId AND ur.RoleId = s.RoleId)
            WHEN NOT MATCHED THEN
            INSERT (UserId, RoleId)
            VALUES (s.UserId, s.RoleId)';
          END;
          /
          {{else}}{{removeLine}}{{/if}}
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Data"

    - fileType: "User Roles Static Class"
      fileName: "UserRoles"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models;
        public static class UserRoles
        {
            public const string Admin = "Admin";
            public const string User = "User";
            // Add another roles if necessary
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models"

    - fileType: "Application User Entity"
      fileName: "ApplicationUser"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models;
        using Microsoft.AspNetCore.Identity;

        public class ApplicationUser : IdentityUser
        {
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models"

    - fileType: "Auth Request DTO"
      fileName: "AuthRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models.Dto;
        public class AuthRequest
        {
            public string Username { get; set; }
            public string Password { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models/Dto"

    - fileType: "Register Request DTO"
      fileName: "RegisterRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models.Dto;
        public class RegisterRequest
        {
            public string Username { get; set; }
            public string Email { get; set; }
            public string Password { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models/Dto"

    - fileType: "Password Change Request DTO"
      fileName: "PasswordChangeRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models.Dto;
        public class PasswordChangeRequest
        {
            public string OldPassword { get; set; }
            public string NewPassword { get; set; }
            public string ConfirmPassword { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models/Dto"

    - fileType: "User Update Request DTO"
      fileName: "UserUpdateRequest"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Models.Dto;
        public class UserUpdateRequest
        {
            public string Name { get; set; }
            public string Email { get; set; }
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Models/Dto"

    - fileType: "User Service"
      fileName: "ApplicationUserService"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Implementation;
        
        using ${majStart(projectName)}.Models.Dto;
        using Microsoft.AspNetCore.Identity;
        using ${majStart(projectName)}.Models;
        
        public class ApplicationUserService
        {
        {{tab}}private readonly UserManager<ApplicationUser> _userManager;
          
        {{tab}}public ApplicationUserService(UserManager<ApplicationUser> userManager)
        {{tab}}{
        {{tab}}    _userManager = userManager;
        {{tab}}}
        
        {{tab}}public async Task<bool> Authenticate(AuthRequest request)
        {{tab}}{
        {{tab}}    var user = await _userManager.FindByNameAsync(request.Username);
        {{tab}}    if (user != null && await _userManager.CheckPasswordAsync(user, request.Password))
        {{tab}}    {
        {{tab}}        return true; // Authentication successful
        {{tab}}    }
        {{tab}}    return false; // Invalid credentials
        {{tab}}}
        
        {{tab}}public async Task<string> Register(RegisterRequest request)
        {{tab}}{
        {{tab}}    var user = new ApplicationUser
        {{tab}}    {
        {{tab}}        UserName = request.Username,
        {{tab}}        Email = request.Email
        {{tab}}    };
        
        {{tab}}    var result = await _userManager.CreateAsync(user, request.Password);
        {{tab}}    if (!result.Succeeded)
        {{tab}}        throw new Exception(string.Join(", ", result.Errors.Select(e => e.Description)));
        
        {{tab}}    return user.Id;
        {{tab}}}
        
        {{tab}}public async Task ChangePassword(string username, PasswordChangeRequest model)
        {{tab}}{
        {{tab}}    var user = await _userManager.FindByNameAsync(username);
        {{tab}}    if (user == null || !await _userManager.CheckPasswordAsync(user, model.OldPassword))
        {{tab}}        throw new Exception("Invalid current password");
        
        {{tab}}    if (model.NewPassword != model.ConfirmPassword)
        {{tab}}        throw new Exception("Passwords do not match");
        
        {{tab}}    await _userManager.ChangePasswordAsync(user, model.OldPassword, model.NewPassword);
        {{tab}}}
        
        {{tab}}public async Task UpdateUser(string username, UserUpdateRequest update)
        {{tab}}{
        {{tab}}    var user = await _userManager.FindByNameAsync(username);
        {{tab}}    if (user == null) throw new Exception("User not found");
        
        {{tab}}    user.Email = update.Email;
        {{tab}}    user.UserName = update.Name;
        {{tab}}    await _userManager.UpdateAsync(user);
        {{tab}}}
        }

      destinationPath: "${destinationFolder}/${majStart(projectName)}/Services/Implementation"

    - fileType: "Authentication Controller"
      fileName: "AuthController"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Controllers;
  
        using Microsoft.AspNetCore.Authorization;
        using Microsoft.AspNetCore.Identity;
        using Microsoft.AspNetCore.Mvc;
        using ${majStart(projectName)}.Models.Dto;
        using ${majStart(projectName)}.Models;
        using ${majStart(projectName)}.Services.Implementation;
  
        [AllowAnonymous]
        public class AuthController : Controller
        {
        {{tab}}private readonly UserManager<ApplicationUser> _userManager;
        {{tab}}private readonly SignInManager<ApplicationUser> _signInManager;
        
        {{tab}}public AuthController(UserManager<ApplicationUser> userManager, SignInManager<ApplicationUser> signInManager)
        {{tab}}{
        {{tab}}    _userManager = userManager;
        {{tab}}    _signInManager = signInManager;
        {{tab}}}
  
        {{tab}}[HttpGet]
        {{tab}}public IActionResult Login()
        {{tab}}{
        {{tab}}    return View();
        {{tab}}}
  
        {{tab}}[HttpPost]
        {{tab}}public async Task<IActionResult> Login(AuthRequest model)
        {{tab}}{
        {{tab}}    if (!ModelState.IsValid)
        {{tab}}        return View(model);
        
        {{tab}}    var result = await _signInManager.PasswordSignInAsync(
        {{tab}}        model.Username, model.Password, isPersistent: false, lockoutOnFailure: false);
        
        {{tab}}    if (result.Succeeded)
        {{tab}}    {{#each allEntities}}
        {{tab}}    {{#if @first}}
        {{tab}}        return RedirectToAction("Index", "${this.entityName}");{{/if}}{{/each}}
        
        {{tab}}    ModelState.AddModelError("", "Invalid username or password");
        {{tab}}    return View(model);
        {{tab}}}
  
        {{tab}}[HttpGet]
        {{tab}}public IActionResult Register()
        {{tab}}{
        {{tab}}    return View();
        {{tab}}}
        
        {{tab}}[HttpPost]
        {{tab}}public async Task<IActionResult> Register(RegisterRequest model)
        {{tab}}{
        {{tab}}    if (!ModelState.IsValid)
        {{tab}}        return View(model);
        
        {{tab}}    var user = new ApplicationUser { UserName = model.Username, Email = model.Email };
        {{tab}}    var result = await _userManager.CreateAsync(user, model.Password);
        
        {{tab}}    if (result.Succeeded)
        {{tab}}        return RedirectToAction("Login");
        
        {{tab}}    foreach (var error in result.Errors)
        {{tab}}        ModelState.AddModelError("", error.Description);
        
        {{tab}}    return View(model);
        {{tab}}}
        
        {{tab}}[HttpPost]
        {{tab}}[Authorize]
        {{tab}}public async Task<IActionResult> Logout()
        {{tab}}{
        {{tab}}    await _signInManager.SignOutAsync();
        {{tab}}    return RedirectToAction("Login");
        {{tab}}}
        }

      destinationPath: "${destinationFolder}/${majStart(projectName)}/Controllers"

    - fileType: "Login Page"
      fileName: "Login"
      extension: "cshtml"
      content: |
        @model ${majStart(projectName)}.Models.Dto.AuthRequest

        @{
            Layout = null;
        }
        
        <!DOCTYPE html>
        <html lang="en">
        <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>${majStart(projectName)} | Log in</title>
            <link rel="stylesheet" href="~/css/style.css" asp-append-version="true" />
            <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }
        
            body {
                background-color: var(--bg-color);
                height: 100vh;
                display: flex;
                justify-content: center;
                align-items: center;
                padding: 20px;
            }
            
            .app-name {
                color: var(--primary-color);
            }
            
            .project-logo {
                width: 100%;
                height: 50px;
                display: flex;
                align-items: center;
                justify-content: center;
                overflow: hidden;
                padding: 5px;
                box-sizing: border-box;
            }
          
            .project-logo img {
                max-width: 90%; 
                max-height: 90%; 
                width: auto;
                height: auto;
                object-fit: contain;
                object-position: center;
                display: block; 
            }
            
            .login-container {
                background-color: var(--bg-secondary);
                border-radius: var(--border-radius);
                box-shadow: var(--shadow-lg);
                width: 100%;
                max-width: 400px;
                overflow: hidden;
            }
            
            .login-header {
                padding: 30px 20px;
                text-align: center;
                color: var(--text-primary);
            }
            
            .login-header h1 {
                font-size: 24px;
                font-weight: 600;
                margin-bottom: 10px;
            }
            
            .login-header p {
                font-size: 14px;
                opacity: 0.9;
            }
            
            .login-form {
                padding: 0px 30px 30px 30px;
            }
            
            .form-group {
                margin-bottom: 20px;
                position: relative;
            }
            
            .form-group label {
                display: block;
                font-size: 14px;
                margin-bottom: 8px;
                color: var(--text-primary);
                font-weight: 500;
            }
            
            .form-group input {
                width: 100%;
                padding: 0.65rem 3rem 0.65rem 1rem;
                border: 1px solid var(--border-color);
                border-radius: var(--border-radius);
                font-size: 14px;
                transition: border-color 0.3s;
            }
            
            .form-group input:focus {
                border-color: var(--primary-color);
                outline: none;
                box-shadow: 0 0 0 2px rgba(102, 126, 234, 0.2);
            }
            
            .input-icon {
                position: absolute;
                right: 1rem;
                top: 72%;
                transform: translateY(-50%);
                color: var(--text-secondary);
            }
            
            .login-form .btn-primary {
                width: 100%;
            }
            
            .error-message {
                margin: 25px;
                color: #e74c3c;
                font-size: 14px;
            }
            
            .password-toggle {
                background: none;
                border: none;
                cursor: pointer;
                padding: 0;
            }
            
            .password-toggle svg {
                display: none;
            }
            
            .password-toggle svg.active {
                display: block;
            }
            </style>
        </head>
        
        <body>
            <div class="login-container">
                <div class="login-header">
        
                {{#if useLogoLink or hasLogo}}
                <div class="project-logo">
                    <img src="~/images${logoUrl}"
                        alt="">
                </div>
                {{else}}
                    <h1 class="app-name">${majStart(projectName)}</h1>
                {{/if}}
        
                </div>
                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="error-message">
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <p>@error.ErrorMessage</p>
                    }
                    </div>
                }
                <form class="login-form" asp-action="Login" method="post">
                    <div class="form-group">
                        <label asp-for="Username" data-i18n="username"></label>
                        <input type="text" asp-for="Username" required>
                        <div class="input-icon">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" viewBox="0 0 24 24">
                                <path fill="currentColor" fill-rule="evenodd" d="M8 11a4 4 0 0 1 4-4h1a4 4 0 0 1 4 4v2a4 4 0 0 1-4 4h-1a4 4 0 0 1-4-4v-2Zm4-2a2 2 0 0 0-2 2v2a2 2 0 0 0 2 2h1a2 2 0 0 0 2-2v-2a2 2 0 0 0-2-2h-1Z" clip-rule="evenodd"></path>
                                <path fill="currentColor" fill-rule="evenodd" d="M3 11.5A8.5 8.5 0 0 1 11.5 3H14a7 7 0 0 1 7 7v4a3 3 0 1 1-6 0v-2h2v2a1 1 0 1 0 2 0v-4a5 5 0 0 0-5-5h-2.5A6.5 6.5 0 0 0 5 11.5v1a6.5 6.5 0 0 0 6.5 6.5h2.923a6.5 6.5 0 0 0 3.344-.926l.718-.432 1.03 1.716-.72.43A8.5 8.5 0 0 1 14.424 21H11.5A8.5 8.5 0 0 1 3 12.5v-1Z" clip-rule="evenodd"></path>
                            </svg>
                        </div>
                    </div>
                
                    <div class="form-group">
                        <label asp-for="Password" data-i18n="password"></label>
                        <input type="password" asp-for="Password" id="password-input" required>
                        <button type="button" class="input-icon password-toggle" id="password-toggle">
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" id="eye-open" class="active">
                                <g fill="none" fill-rule="evenodd" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" transform="translate(1 1)">
                                <path d="M0 8s4-8 11-8 11 8 11 8-4 8-11 8S0 8 0 8z"></path>
                                <circle cx="11" cy="8" r="3"></circle>
                                </g>
                            </svg>
                            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" id="eye-closed">
                                <g fill="none" fill-rule="evenodd" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2">
                                <path d="M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19m-6.72-1.07a3 3 0 1 1-4.24-4.24M1 1l22 22"></path>
                                </g>
                            </svg>
                        </button>
                    </div>
                
                    <button type="submit" class="btn btn-primary btn-md btn-login" data-i18n="login"></button>
                </form>
            </div>
            
            <script>
                document.addEventListener('DOMContentLoaded', function() {
                    const passwordToggle = document.getElementById('password-toggle');
                    const passwordInput = document.getElementById('password-input');
                    const eyeOpen = document.getElementById('eye-open');
                    const eyeClosed = document.getElementById('eye-closed');
                
                    passwordToggle.addEventListener('click', function() {
                        if (passwordInput.type === 'password') {
                            passwordInput.type = 'text';
                            eyeOpen.classList.remove('active');
                            eyeClosed.classList.add('active');
                        } else {
                            passwordInput.type = 'password';
                            eyeClosed.classList.remove('active');
                            eyeOpen.classList.add('active');
                        }
                    });
                });
            </script>
            <script src="~/js/index.js"></script>
            <script src="~/js/lang.js"></script>
        </body>
        </html>

      destinationPath: "${destinationFolder}/${majStart(projectName)}/Views/Auth"

    - fileType: "SQL Schema Bootstrapper Service"
      fileName: "SqlSchemaBootstrapper"
      extension: "cs"
      content: |
        namespace ${majStart(projectName)}.Services.Startup;

        using Microsoft.Extensions.Hosting;
        using Microsoft.Extensions.DependencyInjection;
        using Microsoft.EntityFrameworkCore;
        using System.Data.Common;
        using ${majStart(projectName)}.Data;
        
        public class SqlSchemaBootstrapper : IHostedService
        {
            private readonly IServiceProvider _provider;
            private const string SchemaFilePath = "Data/identity_schema.sql"; // chemin du script SQL

            public SqlSchemaBootstrapper(IServiceProvider provider)
            {
                _provider = provider;
            }

            public async Task StartAsync(CancellationToken cancellationToken)
            {
                using var scope = _provider.CreateScope();
                var context = scope.ServiceProvider.GetRequiredService<${majStart(projectName)}Context>();
                var db = context.Database;
                var conn = db.GetDbConnection();

                try
                {
                    await conn.OpenAsync(cancellationToken);

                    if (!await TableExistsAsync(conn, "AspNetUsers"))
                    {
                        var sql = await File.ReadAllTextAsync(SchemaFilePath, cancellationToken);
                        using var command = conn.CreateCommand();
                        command.CommandText = sql;
                        await command.ExecuteNonQueryAsync(cancellationToken);

                        Console.WriteLine("[Identity Schema] Tables créées automatiquement.");
                    }
                    else
                    {
                        Console.WriteLine("[Identity Schema] Tables déjà existantes.");
                    }
                }
                catch (Exception ex)
                {
                    Console.WriteLine($"[Identity Schema] ERREUR : {ex.Message}");
                }
                finally
                {
                    await conn.CloseAsync();
                }
            }

            public Task StopAsync(CancellationToken cancellationToken) => Task.CompletedTask;

            private async Task<bool> TableExistsAsync(DbConnection connection, string tableName)
            {
                string query = connection.GetType().Name switch
                {
                    var n when n.Contains("Npgsql") => $"SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = '{tableName.ToLower()}');",
                    var n when n.Contains("MySql") => $"SELECT COUNT(*) FROM information_schema.tables WHERE table_name = '{tableName}';",
                    var n when n.Contains("SqlConnection") => $"SELECT COUNT(*) FROM INFORMATION_SCHEMA.TABLES WHERE TABLE_NAME = '{tableName}';",
                    var n when n.Contains("Oracle") => $"SELECT COUNT(*) FROM user_tables WHERE table_name = UPPER('{tableName}');",
                    _ => throw new NotSupportedException($"Type de connexion non reconnu : {connection.GetType().Name}")
                };

                using var cmd = connection.CreateCommand();
                cmd.CommandText = query;
                var result = await cmd.ExecuteScalarAsync();
                return Convert.ToInt32(result) > 0;
            }
        
        }
      destinationPath: "${destinationFolder}/${majStart(projectName)}/Services/Startup"

  additionalDependencies: [ ]
  metadataBooleanTrueKeys:
    - "useAuthCookie"